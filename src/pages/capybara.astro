---
/* No frontmatter needed */
---

<astro:head>
  <title>Why the Capybara? — ParkerPixie</title>
  <meta name="description" content="Why the capybara is my grounding spirit animal, and how Bejeweled the Peacock embodies my glittery, unapologetic side." />
  <link rel="icon" href="/CapyGoldIcon.png" />
</astro:head>

<!-- 🔗 Global site nav -->
<nav class="site-nav">
  <div class="nav-inner">
    <a href="/" class="brand">
      <img src="/CapyGoldIcon.png" alt="ParkerPixie Logo" class="logo" />
      <span>ParkerPixie</span>
    </a>
    <div class="nav-links">
      <a href="/about/">About</a>
      <a href="/contact/">Contact</a>
      <a href="/parkerpixiepaws/">Paws</a>
    </div>
  </div>
</nav>

<main class="why-capybara" role="main">
  <!-- HERO -->
  <header class="hero">
    <div class="eyebrow">Spirit Animals, Sparkle &amp; Healing</div>
    <h1>Why the Capybara?</h1>
    <p class="subtitle">
      The short answer: she found me. The longer answer: I’m equal parts calm capybara and glittering peacock—grounded and unapologetic—guiding my creativity, recovery, and community.
    </p>
  </header>

  <!-- ✨ your existing sections stay the same … -->
</main>

<style>
  :root{
    --brand:#8a5dff;
    --brand-deep:#cbb3ff;
    --gold:#f0c866;
    --ink:#ffffff;
    --ink-soft:#d6d6d6;
    --card:rgba(12,12,20,0.78);
    --card-blur:12px;
    --ring:rgba(138,93,255,0.35);
  }

  :global(body){
    margin:0;
    color: var(--ink);
    font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
    background:
      radial-gradient(1200px 600px at 50% -10%, rgba(138,93,255,.22), transparent 60%),
      radial-gradient(900px 480px at 92% 8%, rgba(240,200,102,.10), transparent 62%),
      linear-gradient(180deg, #090412 0%, #000 70%);
    background-attachment: fixed, fixed, fixed;
  }
  :global(body)::before{
    content:"";
    position:fixed; inset:0; pointer-events:none;
    background: radial-gradient(1px 1px at 2px 2px, rgba(255,255,255,.08), transparent) 0 0/3px 3px;
    mix-blend-mode: screen;
    opacity:.16;
  }

  /* 🔗 NAV STYLES */
  .site-nav {
    background: rgba(0,0,0,0.65);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255,255,255,0.1);
    position: sticky;
    top: 0;
    z-index: 1000;
  }
  .nav-inner {
    max-width: 1080px;
    margin: 0 auto;
    padding: 10px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    color: var(--ink);
    text-decoration: none;
    font-size: 1.1rem;
  }
  .logo { width: 26px; height: 26px; }
  .nav-links a {
    margin-left: 20px;
    text-decoration: none;
    color: var(--ink-soft);
    font-weight: 500;
    transition: color 200ms ease;
  }
  .nav-links a:hover { color: var(--gold); }

  /* ✨ all your existing hero/card/gallery styles remain here … */
</style>
