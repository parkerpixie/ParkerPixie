<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Why the Capybara? — ParkerPixie</title>
  <meta name="description" content="Why the capybara is my grounding spirit animal, and how Bejeweled the Peacock embodies my glittery, unapologetic side." />

  <style>
    :root{
      --brand:#8a5dff;           /* Parker purple */
      --brand-deep:#cbb3ff;      /* light purple accent */
      --gold:#f0c866;            /* soft gold */
      --ink:#ffffff;             /* main body text */
      --ink-soft:#d6d6d6;        /* secondary text */
      --card:rgba(12,12,20,0.78);/* dark frosted card */
      --card-blur:12px;
      --ring:rgba(138,93,255,0.35);
    }

    /* ===== Background like /about (dark, legible, a little glow) ===== */
    html, body { height: 100%; }
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color: var(--ink);
      /* Layered gradients: deep purple glow + subtle gold + black b*
