---
// src/pages/index.astro
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ParkerPixie — Sass · Shimmer · Sparkle</title>
    <meta name="description" content="I stitch joy into amigurumi, decorate digital worlds, and build playful systems that make life feel a little more magical." />
    <link rel="icon" href="/CapyGoldIcon.png" />

    <style>
      :root{
        --bg:#0b0b10;
        --ink:#f6f6fb;
        --muted:#c7c5d6;
        --card:#111119;
        --chip:#262337;
        --chip-ring:#6b4cff;
        --chip-text:#e8ddff;
        --gold:#d6b36b;
        --lilac:#7d5cff;
        --lilac-2:#9b7dff;
        --radius:20px;
      }
      *{box-sizing:border-box}
      html,body{height:100%}
      body{
        margin:0; color:var(--ink);
        background:
          radial-gradient(1200px 600px at 10% -10%, rgba(155,125,255,.15), transparent 60%),
          radial-gradient(900px 600px at 95% 0%, rgba(214,179,107,.12), transparent 60%),
          radial-gradient(900px 900px at 50% 120%, rgba(140,120,255,.10), transparent 60%),
          var(--bg);
        font:16px/1.6 ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
        -webkit-font-smoothing:antialiased;
      }

      .wrap{max-width:1200px; margin:0 auto; padding:22px 20px 80px}

      /* Header --------------------------------------------------------- */
      header.site{
        display:flex; align-items:center; justify-content:space-between; gap:16px;
        padding:10px 0 6px;
      }
      .brand{display:flex; align-items:center; gap:10px; text-decoration:none; color:var(--ink)}
      .brand img{width:34px; height:34px; object-fit:contain; border-radius:10px; filter:drop-shadow(0 3px 10px rgba(214,179,107,.35))}
      .brand .name{font-weight:800; letter-spacing:.3px}

      nav ul{display:flex; gap:22px; list-style:none; margin:0; padding:0; align-items:center}
      nav a{color:#e9e7f7; text-decoration:none}
      nav a:hover{opacity:.85}
      .contact{
        display:inline-flex; align-items:center; padding:8px 16px; border-radius:999px;
        background:linear-gradient(180deg, rgba(214,179,107,.18), rgba(214,179,107,.08));
        border:1px solid rgba(214,179,107,.45);
        color:#fff2cf; box-shadow:0 0 0 2px rgba(214,179,107,.08);
      }

      /* Hero ----------------------------------------------------------- */
      .hero{
        margin-top:12px;
        display:grid; grid-template-columns: 1.05fr .95fr; gap:28px; align-items:start;
      }
      @media (max-width:980px){ .hero{grid-template-columns:1fr} }

      .lead h1{margin:0 0 10px; font-size:clamp(22px, 3.2vw, 28px); font-weight:600; color:#efeefe}
      .big{font-size:clamp(28px, 5vw, 36px); font-weight:700; margin:.2rem 0 1.1rem}
      .chips{display:flex; gap:14px; flex-wrap:wrap; margin:14px 0 16px}
      .chip{
        display:inline-flex; align-items:center; gap:8px; padding:12px 16px;
        border-radius:14px; text-decoration:none; color:var(--chip-text);
        background:radial-gradient(120% 120% at 0% 0%, rgba(125,92,255,.35), rgba(125,92,255,.2) 40%, rgba(125,92,255,.12) 70%, rgba(125,92,255,.08) 100%),
                   linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02));
        border:1px solid rgba(123,99,255,.45);
        box-shadow:0 0 0 2px rgba(123,99,255,.12), inset 0 1px 0 rgba(255,255,255,.05);
      }
      .tagline{margin:10px 0 8px; color:#dddaf0; font-style:italic}
      .heroimg{
        background:linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
        border:1px solid rgba(255,255,255,.08);
        border-radius:22px; padding:10px; box-shadow:0 10px 30px rgba(0,0,0,.35);
      }
      .heroimg img{width:100%; height:auto; display:block; border-radius:16px}

      /* Cards ---------------------------------------------------------- */
      .lanes{margin-top:28px; display:grid; grid-template-columns:repeat(3, 1fr); gap:18px}
      @media (max-width:1100px){ .lanes{grid-template-columns:1fr} }

      .card{
        background:linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.01));
        border:1px solid rgba(255,255,255,.08); border-radius:18px; padding:22px;
        box-shadow:0 10px 28px rgba(0,0,0,.32), inset 0 1px 0 rgba(255,255,255,.04);
      }
      .eyebrow{color:#a9a5c6; text-transform:uppercase; letter-spacing:.2px; font-size:12px; margin-bottom:6px}
      .title{font-size:20px; font-weight:800; margin:0 0 6px}
      .desc{color:#cfcde3; margin:0 0 14px}
      .btn{
        display:inline-flex; align-items:center; gap:8px; padding:10px 14px; border-radius:12px;
        text-decoration:none; color:#f5edff;
        background:linear-gradient(180deg, rgba(125,92,255,.28), rgba(125,92,255,.12));
        border:1px solid rgba(125,92,255,.45); box-shadow:0 0 0 2px rgba(125,92,255,.12);
      }

      /* Footer --------------------------------------------------------- */
      footer{margin-top:34px; color:#bdbad4; font-size:12px}
    </style>
  </head>

  <body>
    <div class="wrap">
      <!-- Header -->
      <header class="site" role="banner">
        <a class="brand" href="/" aria-label="ParkerPixie Home">
          <img src="/CapyGoldIcon.png" alt="Gold capybara icon" />
          <span class="name">ParkerPixie</span>
        </a>
        <nav aria-label="Main">
          <ul>
            <li><a href="/about/">About</a></li>
            <li><a href="/blog/">Blog</a></li>
            <li><a class="contact" href="/contact/">Contact</a></li>
          </ul>
        </nav>
      </header>

      <!-- Hero -->
      <section class="hero" aria-label="Intro">
        <div class="lead">
          <h1>I stitch joy into amigurumi 🧶, decorate digital worlds, and build</h1>
          <div class="big">playful systems that make life feel a little more magical.</div>

          <div class="chips" role="navigation" aria-label="Quick links">
            <a class="chip" href="/about/">Who is ParkerPixie</a>
            <a class="chip" href="/blog/">Blog</a>
            <a class="chip" href="/capybara/">What’s with the Capybara?</a>
          </div>

          <div class="tagline">“Sass, Shimmer, Sparkle”—and a whole lot of heart.</div>
        </div>

        <div class="heroimg" aria-label="Hero image">
          <!-- EXACT file per your repo -->
          <img src="/hero.jpg" alt="Parker knitting with laptop and Steam Deck by the lake" />
        </div>
      </section>

      <!-- Three lanes -->
      <section class="lanes" aria-label="ParkerPixie lanes">
        <!-- Paws -->
        <article class="card">
          <div class="eyebrow">Paws</div>
          <h2 class="title">ParkerPixiePaws</h2>
          <p class="desc">Amigurumi, capybaras, care guides, commissions, and cozy drops.</p>
          <p><a class="btn" href="/parkerpixiepaws/">Enter the Paws Studio →</a></p>
        </article>

        <!-- Pop -->
        <article class="card">
          <div class="eyebrow">Pop</div>
          <h2 class="title">ParkerPixiePop</h2>
          <p class="desc">Gaming, streams, and glittery persona—Infinity Nikki & Dreamlight vibes.</p>
          <p><a class="btn" href="/pop/">See Pop Projects →</a></p>
        </article>

        <!-- Pixel -->
        <article class="card">
          <div class="eyebrow">Pixel</div>
          <h2 class="title">ParkerPixiePixel</h2>
          <p class="desc">Design experiments, printables, and tiny tools that make life easier.</p>
          <p><a class="btn" href="/pixel/">Peek at Pixels →</a></p>
        </article>
      </section>

      <footer>© {year} ParkerPixie — Made with capybara calm & bejeweled sparkle</footer>
    </div>
  </body>
</html>
