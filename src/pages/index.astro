---
// no frontmatter needed
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ParkerPixie — Sass · Shimmer · Sparkle</title>
  <meta name="description" content="ParkerPixie — cozy crochet (Paws), playful pop persona (Pop), and pixel-perfect tinkering (Pixel). Sass · Shimmer · Sparkle ✨" />
  <link rel="icon" href="/img/CapyGoldIcon.png" />
  <style>
    :root{
      --bg: #0b0b0f;
      --panel:#12121a;
      --text:#e9e9f2;
      --muted:#b8b8c7;
      --accent:#b190ff;      /* ethereal lilac */
      --accent-2:#ffd86b;    /* shimmer gold */
      --card:#161622;
      --br:18px;
      --shadow: 0 10px 30px rgba(0,0,0,.35);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      background: radial-gradient(1200px 600px at 10% -10%, #1a1a2e 0%, transparent 60%),
                  radial-gradient(900px 500px at 110% 10%, #241b35 0%, transparent 60%),
                  var(--bg);
      color:var(--text);
      font: 16px/1.5 system-ui, -apple-system, Segoe UI, Roboto, Inter, "Helvetica Neue", Arial, sans-serif;
      letter-spacing:.2px;
    }

    /* ---------- Header ---------- */
    header.site-header{
      position: sticky; top:0; z-index:1000;
      background: linear-gradient(180deg, rgba(10,10,15,.9), rgba(10,10,15,.6) 70%, transparent);
      backdrop-filter: blur(6px);
      border-bottom: 1px solid rgba(255,255,255,.06);
    }
    .nav{
      max-width:1100px; margin:0 auto; padding:14px 18px;
      display:flex; align-items:center; gap:16px;
    }
    .brand{
      display:flex; align-items:center; gap:10px; text-decoration:none; color:var(--text);
      font-weight:800; letter-spacing:.3px;
    }
    .brand img{width:28px; height:28px; display:block}
    .brand span{background:linear-gradient(90deg,var(--accent-2),var(--accent)); -webkit-background-clip:text; background-clip:text; color:transparent}
    nav.links{margin-left:auto; display:flex; gap:18px; flex-wrap:wrap}
    nav.links a{
      text-decoration:none; color:var(--muted); padding:6px 10px; border-radius:10px;
    }
    nav.links a:hover{color:var(--text); background:rgba(255,255,255,.06)}

    /* ---------- Hero ---------- */
    .hero{
      position:relative; isolation:isolate;
      max-width:1100px; margin:46px auto 22px; padding:22px 18px 0;
    }
    .hero h1{
      margin:0 0 6px; font-size:clamp(34px, 4vw + 8px, 64px);
      line-height:1.06; font-weight:900;
      background:linear-gradient(90deg,#fff, #d8d6ff 60%, #ffe8a8);
      -webkit-background-clip:text; background-clip:text; color:transparent;
      text-wrap:balance;
    }
    .hero p{margin:8px 0 18px; color:var(--muted); max-width:70ch}
    .cta{
      display:flex; gap:12px; flex-wrap:wrap; margin:18px 0 28px;
    }
    .btn{
      appearance:none; border:0; cursor:pointer;
      padding:10px 16px; border-radius:12px; font-weight:700;
      background:linear-gradient(90deg,var(--accent), var(--accent-2));
      color:#111; box-shadow:var(--shadow);
      text-decoration:none; display:inline-flex; align-items:center; gap:8px;
    }
    .btn.secondary{
      background:#1d1d2a; color:var(--text); border:1px solid rgba(255,255,255,.08)
    }
    .hero-art{
      position:relative; margin:12px 0 8px;
      border-radius:var(--br); overflow:hidden; background:var(--card);
      box-shadow:var(--shadow);
    }
    .hero-art img{display:block; width:100%; height:auto}
    .eyebrow{color:var(--accent-2); font-weight:700; text-transform:uppercase; letter-spacing:.2em; font-size:.8rem}

    /* ---------- Sections ---------- */
    .grid{
      max-width:1100px; margin:22px auto 80px; padding:0 18px;
      display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:16px;
    }
    .card{
      background:linear-gradient(180deg, #171726, #12121b 60%);
      border:1px solid rgba(255,255,255,.06);
      border-radius:var(--br); padding:18px; box-shadow:var(--shadow);
    }
    .card h3{margin:6px 0 8px; font-size:1.1rem}
    .card p{margin:0; color:var(--muted)}
    .card a{color:var(--accent-2); text-decoration:none}
    .card a:hover{text-decoration:underline}

    /* ---------- Footer ---------- */
    footer{border-top:1px solid rgba(255,255,255,.06); color:var(--muted); text-align:center; padding:22px 14px 40px}

    /* ---------- IMPORTANT: fix invisible overlay clicks ---------- */
    /* Any decorative layer must ignore pointer events. */
    .hero::before, .hero::after, .sparkles, .noise, .grid-bg, .bg, .background {
      pointer-events:none !important;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header class="site-header">
    <div class="nav">
      <a class="brand" href="/">
        <img src="/img/CapyGoldIcon.png" alt="Capybara icon" />
        <span>ParkerPixie</span>
      </a>
      <nav class="links" aria-label="Primary">
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
        <a href="/blog">Blog</a>
        <a href="/capybara">Why the Capybara?</a>
        <a href="/parkerpixiepaws">ParkerPixiePaws</a>
      </nav>
    </div>
  </header>

  <!-- Hero -->
  <main>
    <section class="hero">
      <div class="eyebrow">Sass · Shimmer · Sparkle</div>
      <h1>Cozy crochet, playful pop, and pixel-perfect tinkering.</h1>
      <p>
        I’m Parker—maker of celestial capybaras, Dreamlight Valley fashion, and gently chaotic glitter.
        Come for the Paws (amigurumi), stay for the Pop (streaming), and poke the Pixel (web toys & tools).
      </p>
      <div class="cta">
        <a class="btn" href="/parkerpixiepaws">Shop/Peek: ParkerPixiePaws</a>
        <a class="btn secondary" href="/blog">Read the Blog</a>
      </div>

      <!-- Optional hero art (uses your existing public/img/hero.jpg).
           Replace with any image you like. -->
      <div class="hero-art">
        <img src="/img/hero.jpg" alt="Parker on bed with yarn friends" loading="eager" />
      </div>
    </section>

    <!-- Three feature cards -->
    <section class="grid" aria-label="Features">
      <article class="card">
        <h3>🐾 Paws — Amigurumi</h3>
        <p>Plushies with personality (Clementine, Bejeweled, & more). <a href="/parkerpixiepaws">See the squad →</a></p>
      </article>
      <article class="card">
        <h3>💜 Pop — Streaming</h3>
        <p>Infinity Nikki & Dreamlight Valley design streams. <a href="https://twitch.tv/ParkerPixieDust">Catch a vibe →</a></p>
      </article>
      <article class="card">
        <h3>✨ Pixel — Web Bits</h3>
        <p>Little experiments, templates, and shiny utilities. <a href="/blog">Dive in →</a></p>
      </article>
    </section>
  </main>

  <footer>
    © <span id="y"></span> ParkerPixie · Made with glitter and boundaries.
  </footer>

  <script>
    // year stamp
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
