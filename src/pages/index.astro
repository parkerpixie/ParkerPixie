<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>ParkerPixie — Sass · Shimmer · Sparkle</title>
  <meta name="description" content="ParkerPixie—cozy crochet (Paws), playful pop persona (Pop), and pixel-perfect tinkering (Pixel). Sass · Shimmer · Sparkle ✨" />
  <link rel="icon" href="./assets/CapyGoldIcon.png" />
  <style>
    :root{
      --brand:#8a5dff;          /* Parker purple */
      --brand-deep:#4b2fb5;     /* deeper accent */
      --gold:#D8AA3B;           /* warm gold */
      --ink:#1f1b24;            /* text */
      --bg:#0f0b14;             /* page bg (very deep) */
      --card:#171220;           /* cards */
      --muted:#bdb5d9;
      --ring: 0 0 0 3px rgba(138,93,255,.25);
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0;background:linear-gradient(180deg,#0f0b14,#130e1f 40%,#100b1a);color:#f7f3ff;font-family:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial}
    a{color:inherit;text-decoration:none}
    img{max-width:100%;height:a
