---
// src/pages/blog/lifes-gonna-life.astro
const title = "Life’s Gonna Life";
const subtitle = "Chaos happens. I name it, then move anyway.";
const date = "October 20, 2025";
const tags = ["resilience", "ADHD", "family", "recovery", "Taylor Swift"];

/* Hero image */
const cover = "/img/opalite.png";
const coverAbs = "https://parkerpixie.netlify.app/img/opalite.png";

/* Absolute URL for this page (under /blog/) */
const pageUrl = "https://parkerpixie.netlify.app/blog/lifes-gonna-life/";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title} • ParkerPixie Blog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Life keeps lifing: work upheaval, family swirl, dog ACLs, dead trucks—so I’m practicing naming the chaos, finding the opalite sky, and moving anyway."
    />
    <link rel="icon" href="/CapyGoldIcon.png" />

    <!-- ✅ Absolute canonical under /blog/ -->
    <link rel="canonical" href={pageUrl} />

    <!-- ✅ Social preview (absolute URLs) -->
    <meta property="og:type" content="article" />
    <meta property="og:site_name" content="ParkerPixie" />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:title" content={title + " • ParkerPixie Blog"} />
    <meta property="og:description" content={subtitle} />
    <meta property="og:image" content={coverAbs} />
    <meta property="og:image:alt" content="Opalite sky–inspired hero image" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={subtitle} />
    <meta name="twitter:image" content={coverAbs} />

    <style>
      :root{
        --brand:#8a5dff; --brand-deep:#5a39d9;
        --ink:#f6f1ff; --muted:#cfc7e6;
        --bg:#0e0a16; --card:#151025f2;
        --gold:#f2c98b; --glow:0 0 30px rgba(138,93,255,.35);
        --radius:18px;
      }
      *{box-sizing:border-box}
      html,body{
        margin:0; padding:0; line-height:1.6; color:var(--ink);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji","Segoe UI Emoji";
        background:
          radial-gradient(1200px 800px at 10% -10%, rgba(138,93,255,.16), transparent 60%),
          radial-gradient(900px 600px at 90% 0%, rgba(255,214,232,.14), transparent 58%),
          url('/bg/cotton-candy.jpg') center/cover no-repeat fixed,
          var(--bg);
      }
      a{ color:var(--gold); text-decoration:none }
      a:hover{ text-decoration:underline }
      .wrap{max-width:1080px; margin:0 auto; padding:0 20px}
      header.site{
        position:sticky; top:0; z-index:50;
        backdrop-filter: blur(10px);
        background: linear-gradient(to bottom, rgba(15,11,20,.86), rgba(15,11,20,.55));
        border-bottom:1px solid rgba(255,255,255,.08);
      }
      .nav{display:flex; align-items:center; justify-content:space-between; gap:16px; padding:12px 0}
      .brand{display:flex; align-items:center; gap:12px; font-weight:800; letter-spacing:.3px}
      .brand img{width:28px; height:28px; filter:drop-shadow(0 0 8px rgba(242,201,139,.45))}
      .brand span{font-size:18px; color:var(--gold)}
      .nav-right{display:flex; align-items:center; gap:14px}
      .nav-link{
        padding:.55rem .2rem; border-radius:8px; opacity:.92;
        transition:opacity .15s ease, color .15s ease, background .15s ease;
        color:#fff; text-decoration:none;
      }
      .nav-link:hover{opacity:1; color:var(--gold)}
      .nav-btn{
        padding:.6rem .9rem; border-radius:999px; font-weight:600;
        border:1px solid rgba(255,255,255,.18);
        background:linear-gradient(135deg,var(--brand),var(--brand-deep));
        box-shadow:0 8px 24px rgba(90,57,217,.35);
        color:#fff; text-decoration:none;
      }
      .nav-btn:hover{filter:brightness(1.06)}
      @media (max-width:900px){
        .nav-right{gap:10px}
        .nav-link{padding:.5rem .1rem}
      }
      .shell{ max-width:820px; margin:40px auto 100px; padding:0 20px }
      header.post{
        background: linear-gradient(180deg, rgba(138,93,255,.18), transparent 55%), var(--card);
        border:1px solid rgba(255,255,255,.10); border-radius:var(--radius);
        box-shadow:var(--glow); padding:28px; position:relative; overflow:hidden;
      }
      .sparkles{
        position:absolute; inset:-2px; pointer-events:none; opacity:.5; filter:blur(.2px);
        background:
          radial-gradient(8px 8px at 20% 30%, rgba(242,201,139,.8), transparent 60%),
          radial-gradient(6px 6px at 70% 20%, rgba(138,93,255,.65), transparent 60%),
          radial-gradient(5px 5px at 85% 65%, rgba(242,201,139,.7), transparent 60%),
          radial-gradient(7px 7px at 35% 80%, rgba(138,93,255,.45), transparent 60%);
      }
      .kicker{ color:var(--muted); letter-spacing:.12em; text-transform:uppercase; font-size:.78rem }
      h1{ font-size:clamp(1.8rem, 4vw, 2.6rem); line-height:1.12; margin:8px 0 6px; color:#fff }
      .sub{ color:#e8e0ff; font-size:clamp(1rem, 2.2vw, 1.15rem); margin:0 0 14px }
      .meta{ display:flex; flex-wrap:wrap; gap:10px; align-items:center; color:var(--muted); font-size:.92rem }
      .tags{ display:flex; gap:8px; flex-wrap:wrap; margin-left:auto }
      .tag{ background:rgba(242,201,139,.12); border:1px solid rgba(242,201,139,.35);
            color:#ffeecf; padding:4px 10px; border-radius:999px; font-size:.78rem }
      .cover{ margin:18px 0 0; border-radius:calc(var(--radius) - 6px); overflow:hidden; border:1px solid rgba(255,255,255,.12) }
      .cover img{ width:100%; display:block }
      .content{
        margin-top:26px; padding:26px; border-radius:var(--radius);
        background: rgba(9,7,15,.55); backdrop-filter: blur(4px);
        border:1px solid rgba(255,255,255,.10);
      }
      .insight{
        display:grid; grid-template-columns:48px 1fr; gap:14px; align-items:center;
        background: linear-gradient(180deg, rgba(242,201,139,.12), rgba(138,93,255,.10));
        border:1px dashed rgba(242,201,139,.45); padding:14px 16px; border-radius:14px; margin:8px 0 22px;
      }
      .insight .badge{
        width:48px; height:48px; border-radius:12px; display:grid; place-items:center;
        background: radial-gradient(circle at 30% 25%, #fff7, transparent 40%), #2a2141;
        color:#ffe8bd; font-weight:700; font-size:.68rem; text-align:center; line-height:1.1;
        border:1px solid rgba(255,255,255,.18);
      }
      .pull{
        font-size:clamp(1.05rem, 2.2vw, 1.25rem);
        color:#fff; background:rgba(138,93,255,.16);
        border-left:3px solid var(--brand); padding:14px 16px; border-radius:12px; margin:20px 0;
      }
      .hr{ height:1px; background:linear-gradient(90deg, transparent, rgba(255,255,255,.22), transparent); margin:22px 0 }
      .mantra{
        display:flex; gap:10px; align-items:center; color:#fff; font-weight:600;
        background:#1c1530; border:1px solid rgba(255,255,255,.14); border-radius:12px; padding:12px 14px; margin-top:18px;
      }
      .mantra .dot{ width:9px; height:9px; border-radius:99px; background:var(--gold); box-shadow:0 0 10px rgba(242,201,139,.9) }
      .foot{ color:var(--muted); font-size:.9rem; display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap; margin-top:18px }
      .share{ display:flex; gap:10px; flex-wrap:wrap }
      .btn{
        padding:7px 12px; border-radius:999px; border:1px solid rgba(255,255,255,.18);
        background:#1b132e; color:#f3ecff; font-size:.85rem; cursor:pointer; text-decoration:none;
        transition: transform .06s ease, border-color .18s ease;
      }
      .btn:hover{ border-color:rgba(242,201,139,.6) }
      .btn:active{ transform: translateY(1px); }
      #toast{
        position: fixed; left: 50%; bottom: 24px; transform: translateX(-50%) translateY(10px);
        background: rgba(28, 21, 48, .95);
        color: #f6f1ff; border: 1px solid rgba(255,255,255,.18);
        padding: 10px 14px; border-radius: 999px; opacity: 0; pointer-events: none;
        transition: opacity .18s ease, transform .18s ease; box-shadow: 0 10px 30px #0007;
        font-size: .92rem;
      }
      #toast.show{ opacity: 1; transform: translateX(-50%) translateY(0); }
    </style>
  </head>
  <body>
    <header class="site">
      <div class="wrap nav">
        <a class="brand" href="/">
          <img src="/CapyGoldIcon.png" alt="ParkerPixie gold capybara icon" />
          <span>ParkerPixie</span>
        </a>
        <nav class="nav-right" aria-label="Primary">
          <a class="nav-link" href="/about/">About</a>
          <a class="nav-link" href="/blog/">Blog</a>
          <a class="nav-btn" href="/contact/">Contact</a>
        </nav>
      </div>
    </header>

    <main class="shell">
      <header class="post">
        <div class="sparkles" aria-hidden="true"></div>
        <div class="kicker">ParkerPixie Blog</div>
        <h1>{title} ✨</h1>
        <p class="sub">{subtitle}</p>
        <div class="meta">
          <span>{date}</span>
          <div class="tags">
            {tags.map((t) => <span class="tag">{t}</span>)}
          </div>
        </div>
        {cover && (
          <figure class="cover">
            <img src={cover} alt="Opalite sky–inspired hero image" loading="eager" />
          </figure>
        )}
      </header>

      <article class="content">
        <section class="insight" role="note" aria-label="Pixie Insight">
          <div class="badge">Pixie<br/>Insight</div>
          <p style="margin:0;">Sometimes the bravest thing you can do is look at the chaos, shrug, and whisper: “Welp, this sucks,” before taking the next tiny step anyway.</p>
        </section>

        <p>Lately, I’ve been dodging life’s curveballs like it’s a competitive sport.</p>

        <p>
          Work has been a revolving door of change — my boss left, new projects keep dropping in my lap, and the deadlines multiply faster than
          laundry on a Sunday. Meanwhile, home looks like a sitcom written by Mercury in retrograde: parenting a neurodivergent teen who reminds me
          how irritating I must have been as a kid, a dog with a torn ACL, and a truck that decided it no longer believes in starting.
        </p>

        <p>
          Somewhere in the chaos, I was reminded of an old Talbot family story. Supposedly, my husband’s ancestor Peter Talbot was shanghaied to the
          “New World,” missed his boat back, caught the next one, and then the ship sank. The family legend goes that bad luck runs deep in Talbot blood — not in
          neat little threes like everyone else’s misfortunes, but in never-ending loops.
        </p>

        <p>Honestly, it tracks.</p>

        <p>
          But here’s the thing I’m learning: <strong>life is going to life.</strong> Whether I like it or not. Whether it’s convenient, kind, or emotionally affordable — life will keep lifing.
          Change is inevitable. Always. Forever. Amen.
        </p>

        <p>
          And yeah, it sucks. Sometimes I cry. Sometimes I swear. Sometimes I spiral and then remember I’m too tired to spiral. But once I burn through the frustration,
          I’m left with the one thing I can actually control: <strong>my reaction.</strong>
        </p>

        <h2>What I Can Control</h2>
        <p>When chaos hits, I have to pause long enough to ask:</p>
        <ul>
          <li>What’s actually within my control?</li>
          <li>What can I let go of, because fighting it won’t fix it?</li>
          <li>What really matters today — what are my “big rocks”?</li>
        </ul>

        <p>Sometimes it’s a thought like, <em>“OMG, stop talking, you’re digging a hole.”</em><br/>
           Sometimes it’s, <em>“What positive can I pull from this?”</em><br/>
           Sometimes it’s just taking a breath long enough to remember I’m not actually on fire.</p>

        <h2>Case Study: This Morning</h2>
        <p>When I woke up today, my brain sounded like a squirrel on Red Bull:</p>
        <ul>
          <li>Porter had no lunch for school.</li>
          <li>Blake’s truck wouldn’t start.</li>
          <li>Two high-profile work emails due, zero assets to work with.</li>
          <li>Counseling appointment at 8:15 AM.</li>
          <li>Vet visit for Ozzy’s maybe-torn ACL (confirmed later: yep).</li>
          <li>No groceries. No plan for dinner.</li>
          <li>Need to get to the DMV for Porter’s learner’s permit (God help Wisconsin).</li>
        </ul>

        <p>It was chaos before coffee.</p>

        <p>But after a few deep breaths, I realized:</p>
        <ol type="A">
          <li>I can’t send those emails without assets, so stop panicking.</li>
          <li>I <em>can</em> grocery shop and get Ozzy to the vet. <strong>(Done — Ozzy’s got a torn ACL, but at least it won’t cost us an arm and a leg.)</strong></li>
          <li>Counseling was right on time — not an inconvenience, a lifeline.</li>
          <li>Blake found a frozen pizza. (Dinner: handled.)</li>
          <li>The rest will fall into place. It always does.</li>
        </ol>

        <h2>The Epiphany</h2>
        <p>Here’s what I came to today:</p>
        <p>
          Talbot family lore or not, work change or not, all the neurodivergence you can pack into a three-person household or not —
          <strong>change is going to happen.</strong> Curveballs are going to come.
        </p>

        <p>
          Life was never meant to be a constant. Like biology, it evolves. Things shift. Species adapt. Habits dissolve and reform. Some days it’s easy
          to flow with it; other days it’s brutally hard — especially when your ADHD-brain clings to cozy routines like they’re life rafts. But that’s the deal.
          That’s the dance.
        </p>

        <section class="insight" role="note" aria-label="Pixie Insight">
          <div class="badge">Pixie<br/>Insight</div>
          <p style="margin:0;">“We are not thinking machines that feel — we are feeling machines that think.”<br/><em>— Brené Brown, <cite>Atlas of the Heart</cite></em></p>
        </section>

        <p>
          That line stopped me cold. Because that’s it, isn’t it? We keep trying to <em>think</em> our way through the mess, but maybe the trick is to <em>feel</em> our way
          through it — to let emotion guide the logic instead of apologizing for it.
        </p>

        <p><strong>Life is going to life.</strong></p>

        <p>
          I can’t stop the chaos, but I can name it.<br/>
          I can’t control the lightning, but I can look for the opalite sky it leaves behind.<br/>
          And if you caught that Taylor Swift reference — you know why her lyrics cut deeper than anyone gives her credit for.
        </p>

        <div class="hr"></div>

        <p class="mantra">
          <span class="dot" aria-hidden="true"></span>
          <span><strong>Mantra:</strong> Life’s gonna life. I’m gonna live anyway.</span>
        </p>

        <footer class="foot">
          <div>— Parker</div>
          <nav class="share" aria-label="Share this post">
            <button class="btn" id="copyLinkBtn" type="button">Copy Link</button>
            <a class="btn" id="fbShare" href="#" rel="noopener">Share on Facebook</a>
            <button class="btn" id="nativeShareBtn" type="button" hidden>Share…</button>
            <a class="btn" href="/blog/">Back to Blog</a>
          </nav>
        </footer>
      </article>
    </main>

    <div id="toast" role="status" aria-live="polite" aria-atomic="true">Link copied ✨</div>

    <script type="module">
      const url = new URL(window.location.href);
      const titleMeta = document.title || "ParkerPixie Blog";

      const copyBtn = document.getElementById("copyLinkBtn");
      const fbA = document.getElementById("fbShare");
      const nativeBtn = document.getElementById("nativeShareBtn");
      const toast = document.getElementById("toast");

      const fbShareUrl = new URL("https://www.facebook.com/sharer/sharer.php");
      fbShareUrl.searchParams.set("u", url.href);
      fbA.href = fbShareUrl.toString();

      copyBtn?.addEventListener("click", async () => {
        try {
          if (navigator.clipboard?.writeText) {
            await navigator.clipboard.writeText(url.href);
          } else {
            const ta = document.createElement("textarea");
            ta.value = url.href;
            document.body.appendChild(ta);
            ta.select();
            document.execCommand("copy");
            ta.remove();
          }
          showToast();
        } catch {
          window.prompt("Copy this URL:", url.href);
        }
      });

      if (navigator.share) {
        nativeBtn.hidden = false;
        nativeBtn.addEventListener("click", () => {
          navigator.share({ title: titleMeta, url: url.href }).catch(() => {});
        });
      }

      let toastTimer;
      function showToast() {
        toast.classList.add("show");
        clearTimeout(toastTimer);
        toastTimer = setTimeout(() => toast.classList.remove("show"), 1400);
      }
    </script>
  </body>
</html>
