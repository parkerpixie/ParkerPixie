---
// src/pages/blog/helloworld.astro
export const prerender = true;

const title = "Hello, Sparkle World";
const description = "Soft launch of Parker’s daily mantra post format.";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- GLOBAL styles so the background actually applies -->
    <style is:global>
      :root{
        --pp-glass: rgba(255,255,255,.60);
        --pp-glass-border: rgba(255,255,255,.45);
        --pp-deep: #1f1536;
        --pp-shadow: 0 12px 40px rgba(31,21,54,.18);
        --pp-g1:#f7e7b4; --pp-g2:#f1c86d; --pp-g3:#e8a83b; /* golds */
      }

      html,body{height:100%;margin:0}
      body{color:var(--pp-deep);}

      /* full-page background (with gradient fallback) */
      body::before{
        content:""; position:fixed; inset:0; z-index:-2;
        background:
          linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,0)),
          url("/bg/cotton-candy.jpg?v=1") center/cover no-repeat fixed;
        /* if image missing, you'll still get the gradient layer above */
      }
      /* soft bloom for readability */
      body::after{
        content:""; position:fixed; inset:0; z-index:-1;
        background:
          radial-gradient(60% 35% at 50% 0%, rgba(255,255,255,.35), transparent 60%),
          radial-gradient(70% 40% at 50% 100%, rgba(255,255,255,.35), transparent 60%);
        pointer-events:none;
      }

      /* glass card */
      .page{
        max-width:820px; margin:56px auto; padding:28px 28px 30px;
        background:var(--pp-glass);
        backdrop-filter: blur(12px) saturate(1.05);
        -webkit-backdrop-filter: blur(12px) saturate(1.05);
        border:1px solid var(--pp-glass-border);
        border-radius:22px; box-shadow: var(--pp-shadow);
      }

      /* pill + paragraph */
      .pp-insight{ margin:0 0 20px; }
      .pp-badge{
        display:inline-flex; align-items:center; gap:.5rem;
        padding:.5rem .9rem; border-radius:999px; font-weight:700; font-size:.9rem;
        color:#1f1536; border:1px solid rgba(255,255,255,.7);
        background:
          radial-gradient(80% 120% at 10% 10%, rgba(255,255,255,.85), rgba(255,255,255,.3) 60%, transparent 70%),
          linear-gradient(90deg,var(--pp-g1),var(--pp-g2),var(--pp-g3));
        background-size:200% 100%;
        animation: sheen 6s ease-in-out infinite;
        box-shadow: 0 6px 18px rgba(0,0,0,.08);
      }
      @keyframes sheen{0%,100%{background-position:0 0}50%{background-position:100% 0}}

      h1{ margin:.2rem 0 1rem; font-size:clamp(1.8rem,2.5vw + 1rem,2.6rem); line-height:1.15; }
      p{ font-size:1.05rem; }
    </style>
  </head>
  <body>
    <main class="page">
      <aside class="pp-insight">
        <div class="pp-badge">Mantra for the Day</div>
        <p>Rest isn’t quitting; it’s healing on purpose.</p>
      </aside>

      <h1>{title}</h1>
      <p>First post vibes. Testing the layout, the grid, the mantra pill, and the sparkle.</p>
    </main>
  </body>
</html>
