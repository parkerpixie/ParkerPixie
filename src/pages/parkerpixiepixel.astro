---
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ParkerPixiePixel • Templates & Designs</title>
  <meta name="description" content="Playful, ADHD-friendly templates and graphics that make planning feel magical." />
  <link rel="icon" href="/CapyGoldIcon.png" />

  <!-- ✅ Open Graph for Facebook -->
  <link rel="canonical" href="https://parkerpixie.netlify.app/parkerpixiepixel/" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://parkerpixie.netlify.app/parkerpixiepixel/" />
  <meta property="og:title" content="ParkerPixiePixel — Templates & Designs" />
  <meta property="og:description" content="Playful, ADHD-friendly templates and graphics that make planning feel magical." />
  <meta property="og:image" content="https://parkerpixie.netlify.app/hero.jpg" />
  <meta property="og:image:alt" content="ParkerPixiePixel hero image" />

  <!-- (Optional) Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="ParkerPixiePixel — Templates & Designs" />
  <meta name="twitter:description" content="Playful, ADHD-friendly templates and graphics that make planning feel magical." />
  <meta name="twitter:image" content="https://parkerpixie.netlify.app/hero.jpg" />

  <style>
    /* ======= Brand variables (pink/purple re-theme) ======= */
    :root{
      /* Core brand */
      --pp-pink:#ff6fc6;
      --pp-pink-2:#ff8fd5;
      --pp-lilac:#b479ff;
      --pp-violet:#7a5cff;
      --pp-deep:#1b0f2b;

      /* Text colors */
      --ink:#f6edff;             /* light ink on dark */
      --muted:#daccff;           /* soft lavender */
      --ink-dark:#2c2c2c;        /* ink on light cards */

      /* Surfaces */
      --bg-card:#f5f1eb;
      --ring:rgba(255,255,255,.18);

      /* Carousel */
      --rail-pad:1rem;
      --card-w:320px;
      --gap:16px;
    }

    body{
      margin:0;
      font-family:'Segoe UI', ui-sans-serif, system-ui, -apple-system, Roboto, Helvetica, Arial;
      color:var(--ink);
      background:#000;
      position:relative;
      min-height:100vh;
      z-index:1;
    }

    /* 🌟 Full-page background video */
    .bg-video{position:fixed; inset:0; width:100%; height:100%; object-fit:cover; z-index:-1}

    /* ===== Header (pink→violet glass bar) ===== */
    header{
      padding:1rem 2rem;
      background:
        linear-gradient(180deg, color-mix(in oklab, var(--pp-violet) 40%, transparent), color-mix(in oklab, var(--pp-pink) 35%, transparent));
      border-bottom:1px solid rgba(255,255,255,.22);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      display:flex; justify-content:space-between; align-items:center;
      position:relative; z-index:2;
      color:var(--ink);
    }
    header a{ color:var(--ink); margin-left:1rem; font-weight:700; text-decoration:none }
    header a:hover{ text-decoration:underline }

    /* ===== Hero ===== */
    .hero{ text-align:center; padding:6rem 2rem 2rem; position:relative; z-index:2 }
    .hero-bubble{
      max-width:1000px; margin:0 auto; padding:24px 22px; border-radius:24px;
      background:
        radial-gradient(900px 400px at 10% -10%, color-mix(in oklab, var(--pp-pink) 22%, transparent), transparent 60%),
        radial-gradient(800px 300px at 90% 0%, color-mix(in oklab, var(--pp-violet) 22%, transparent), transparent 55%),
        rgba(255,255,255,.12);
      border: 1px solid rgba(255,255,255,.28);
      box-shadow: 0 20px 60px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.35);
      -webkit-backdrop-filter: blur(10px); backdrop-filter: blur(10px);
    }
    .hero h1{ font-size:3rem; font-weight:800; color:#ffe6f7; margin:0 0 12px; text-shadow:0 2px 12px rgba(0,0,0,.35) }
    .hero p { max-width:700px; font-size:1.15rem; color:var(--muted); margin:0 auto 18px }
    .btn-primary{
      display:inline-block; text-decoration:none; font-weight:800; letter-spacing:.2px;
      padding:.9rem 1.8rem; border-radius:9999px; color:#140a1c;
      background: linear-gradient(135deg, var(--pp-pink) 0%, var(--pp-lilac) 100%);
      border:1px solid color-mix(in oklab, var(--pp-pink) 70%, #000);
      box-shadow: 0 10px 24px rgba(122,92,255,.35), inset 0 0 0 1px rgba(255,255,255,.2);
    }
    .btn-primary:hover{ filter:brightness(1.05) }

    /* ===== Filter bubbles ===== */
    .filter-bubbles{ display:flex; flex-wrap:wrap; justify-content:center; gap:.5rem; padding:1rem 2rem; position:relative; z-index:2 }
    .filter-btn{
      background: color-mix(in oklab, var(--pp-violet) 18%, transparent);
      color:#fff; border:1px solid rgba(255,255,255,.22);
      padding:.5rem 1rem; border-radius:9999px; font-weight:700; cursor:pointer; transition:all .2s;
    }
    .filter-btn:hover{ background: color-mix(in oklab, var(--pp-pink) 30%, var(--pp-violet) 20%) }
    .filter-btn.active{ background: linear-gradient(135deg, var(--pp-violet), var(--pp-pink)); transform:scale(1.05) }

    /* ===== Button-only carousel (no native scroll) ===== */
    .templates-outer{ position:relative; z-index:2; padding:0 var(--rail-pad) 3rem; max-width:1400px; margin:0 auto }
    .templates-viewport{ overflow:hidden; position:relative }
    .templates-rail{ display:flex; gap:var(--gap); padding:1rem 0; transform:translateX(0); will-change:transform; transition:transform .35s ease }

    .tcard{
      flex:0 0 var(--card-w); width:var(--card-w);
      background: var(--bg-card); color:var(--ink-dark);
      border:1px solid rgba(0,0,0,.08);
      border-radius:16px; overflow:hidden;
      box-shadow: 0 10px 24px rgba(0,0,0,.28);
      display:flex; flex-direction:column;
      transition: transform .18s ease, box-shadow .18s ease;
    }
    .tcard:hover{ transform: translateY(-2px); box-shadow: 0 16px 28px rgba(0,0,0,.32) }
    .tcard-meta{ padding:.75rem 1rem .25rem; font-weight:800; color:#5b2abf }
    .tcard-media{ line-height:0; background:#fff; border-top:1px solid rgba(0,0,0,.06); border-bottom:1px solid rgba(0,0,0,.06) }
    .tcard-media img{ display:block; width:100%; height:auto; aspect-ratio:4/3; object-fit:contain; image-rendering:-webkit-optimize-contrast }
    .tcard-body{ padding:1rem; display:flex; flex-direction:column; gap:.5rem; flex:1 }
    .tcard-title{ margin:0; font-size:1.05rem; color:#4b2fb5; font-weight:800 }
    .tcard-desc{ margin:0; color:#5a5a5a; font-size:.94rem; line-height:1.35; flex:1 }
    .tcard-actions{ margin-top:.6rem }
    .save-btn{
      display:inline-block; background: linear-gradient(135deg, var(--pp-pink), var(--pp-lilac));
      color:#160a1e; text-decoration:none; padding:.55rem 1rem; border-radius:9999px; font-weight:800;
      border:1px solid color-mix(in oklab, var(--pp-pink) 70%, #000);
    }
    .save-btn:hover{ filter:brightness(1.05) }

    .is-hidden{ display:none !important }

    /* Arrows */
    .rail-btn{
      position:absolute; top: calc(50% - 24px);
      width:46px; height:46px; border-radius:50%;
      border:1px solid rgba(255,255,255,.25);
      background: color-mix(in oklab, var(--pp-violet) 35%, rgba(255,255,255,.2));
      color:#fff; font-weight:900; font-size:20px;
      display:grid; place-items:center; backdrop-filter: blur(6px); cursor:pointer; user-select:none;
    }
    .rail-btn:hover{ filter:brightness(1.08) }
    .rail-prev{ left:4px } .rail-next{ right:4px }
    .rail-btn[disabled]{ opacity:.4; cursor:not-allowed }
    @media (max-width:720px){ .rail-btn{ display:none } }

    /* ===== Secret Burrow (after gallery) ===== */
    .burrow-section{ max-width:1000px; margin: 0 auto 50px; padding: 0 16px; text-align:center }
    .burrow-pill{
      display:inline-flex; align-items:center; gap:.6rem; padding: 12px 18px; border-radius: 9999px;
      font: 900 16px/1 ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Inter, "Helvetica Neue", Arial;
      color:#fff;
      background: linear-gradient(135deg, var(--pp-violet), var(--pp-pink));
      border: 1px solid rgba(255,255,255,.45);
      box-shadow: 0 10px 30px rgba(122,92,255,.35), inset 0 1px 0 rgba(255,255,255,.35);
      text-decoration:none;
    }
    .burrow-pill:hover{ filter:brightness(1.06); transform: translateY(-1px) }

    /* NEW: Black secondary button under the pill */
    .btn-dark{
      display:inline-block; margin-top:10px;
      background:#0f0f12; color:#fff; text-decoration:none;
      padding:.65rem 1.1rem; border-radius:9999px; font-weight:800; letter-spacing:.2px;
      border:1px solid rgba(255,255,255,.25);
      box-shadow:0 8px 20px rgba(0,0,0,.45);
      cursor:pointer;
    }
    .btn-dark:hover{ background:#141419 }
    .copy-note{ display:block; margin-top:8px; color:var(--muted); font-weight:700; }

    footer{ text-align:center; padding:2rem; color:#fff; font-size:.85rem; position:relative; z-index:2; text-shadow:0 2px 4px rgba(0,0,0,0.6) }
  </style>
</head>
<body>

  <!-- Background video -->
  <video autoplay muted loop playsinline poster="/img/hero-fallback.jpg" class="bg-video">
    <source src="/videos/pixelbackground.webM" type="video/webm" />
    <source src="/videos/Pixelbackground.mp4" type="video/mp4" />
  </video>

  <!-- Header -->
  <header>
    <a href="/">ParkerPixie ✨</a>
    <nav>
      <a href="/about">About</a>
      <a href="/blog">Blog</a>
      <a href="/contact">Contact</a>
    </nav>
  </header>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-bubble">
      <h1>ParkerPixiePixel</h1>
      <p>
        I design playful, ADHD-friendly templates and tools that transform everyday planning
        into something beautiful and engaging. By blending creativity with organization,
        I help families (starting with my own!) stay on track without losing the magic.
      </p>
      <a href="https://your-google-form-link.com" class="btn-primary">Request a Graphic</a>
    </div>
  </section>

  <!-- Filters -->
  <div class="filter-bubbles">
    <button class="filter-btn active" data-filter="all">All</button>
    <button class="filter-btn" data-filter="meal">Meal Planning</button>
    <button class="filter-btn" data-filter="adhd">ADHD Tool</button>
    <button class="filter-btn" data-filter="home">Home Management</button>
    <button class="filter-btn" data-filter="banner">Banners</button>
    <button class="filter-btn" data-filter="art">Artwork</button>
    <button class="filter-btn" data-filter="travel">Travel</button>
  </div>

  <!-- Carousel -->
  <section class="templates-outer" aria-label="Templates gallery">
    <button class="rail-btn rail-prev" type="button" id="prevBtn" title="Previous">‹</button>
    <button class="rail-btn rail-next" type="button" id="nextBtn" title="Next">›</button>

    <div class="templates-viewport">
      <div class="templates-rail" id="rail">
        <!-- Meal Planning -->
        <article class="tcard" data-category="meal">
          <div class="tcard-meta">Meal Planning</div>
          <div class="tcard-media">
            <img src="/img/dummy-1200x800.jpg" alt="Preview — Weekly Meal Planner" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Weekly Meal Planner</h3>
            <p class="tcard-desc">A simple ADHD-friendly planner with room for meals, snacks, and grocery notes.</p>
            <div class="tcard-actions"><a href="/img/dummy-1200x800.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>

        <!-- ADHD Tool -->
        <article class="tcard" data-category="adhd">
          <div class="tcard-meta">ADHD Tool</div>
          <div class="tcard-media">
            <img src="/img/dummy-512x512.jpg" alt="Preview — Focus Dashboard" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Focus Dashboard</h3>
            <p class="tcard-desc">Color-coded blocks and gentle prompts to reduce friction and boost follow-through.</p>
            <div class="tcard-actions"><a href="/img/dummy-512x512.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>

        <!-- Home Management -->
        <article class="tcard" data-category="home">
          <div class="tcard-meta">Home Management</div>
          <div class="tcard-media">
            <img src="/img/dummy-600x400.jpg" alt="Preview — Family Chore Chart" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Family Chore Chart</h3>
            <p class="tcard-desc">A colorful, easy-to-read chart that keeps chores on track without nagging.</p>
            <div class="tcard-actions"><a href="/img/dummy-600x400.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>

        <!-- Banners -->
        <article class="tcard" data-category="banner">
          <div class="tcard-meta">Banners</div>
          <div class="tcard-media">
            <img src="/img/dummy-1200x628.jpg" alt="Preview — Social Share Banner" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Social Share Banner</h3>
            <p class="tcard-desc">Plug-and-play OpenGraph image that looks crisp on Facebook, X, and LinkedIn.</p>
            <div class="tcard-actions"><a href="/img/dummy-1200x628.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>

        <!-- Artwork -->
        <article class="tcard" data-category="art">
          <div class="tcard-meta">Artwork</div>
          <div class="tcard-media">
            <img src="/img/dummy-1920x1080.jpg" alt="Preview — Hero Canvas" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Hero Canvas</h3>
            <p class="tcard-desc">HD hero backdrop test for sections that need a dramatic moment without blur.</p>
            <div class="tcard-actions"><a href="/img/dummy-1920x1080.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>

        <!-- Travel -->
        <article class="tcard" data-category="travel">
          <div class="tcard-meta">Travel</div>
          <div class="tcard-media">
            <img src="/img/dummy-3840x2160.jpg" alt="Preview — Road Trip Itinerary" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/img/dummy-512x512.jpg';" />
          </div>
          <div class="tcard-body">
            <h3 class="tcard-title">Road Trip Itinerary</h3>
            <p class="tcard-desc">Plan stops, sights, and snacks with an adventure-map vibe.</p>
            <div class="tcard-actions"><a href="/img/dummy-3840x2160.jpg" download class="save-btn">Save</a></div>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- ✨ Secret Burrow -->
  <section class="burrow-section" aria-label="Secret Burrow">
    <a href="#" class="burrow-pill" id="burrowShare" aria-label="Share on Facebook to claim your prize">
      <span aria-hidden="true">✨</span>
      Share on Facebook to claim your prize
      <span aria-hidden="true">🧵</span>
    </a>

    <!-- Black secondary action under the pill -->
    <button class="btn-dark" id="copyShareLink" type="button">Copy share link</button>
    <small class="copy-note" id="copyNote" hidden>Link copied!</small>
  </section>

  <footer>
    © {year} ParkerPixie • Sparkles, Pixels & Organization
  </footer>

  <script>
    /* ===== Button-only carousel logic ===== */
    const rail = document.getElementById('rail');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    const GAP = parseInt(getComputedStyle(document.documentElement).getPropertyValue('--gap')) || 16;
    const CARD_W = parseInt(getComputedStyle(document.documentElement).getPropertyValue('--card-w')) || 320;
    let index = 0;

    const visibleCards = () => Array.from(rail.querySelectorAll('.tcard')).filter(el => !el.classList.contains('is-hidden'));
    const clamp = (n,min,max)=>Math.max(min,Math.min(n,max));
    function updateButtons(){ const v = visibleCards(); prevBtn.disabled = (index<=0); nextBtn.disabled = (index >= Math.max(0, v.length-1)); }
    function snapTo(i){ const v = visibleCards(); index = clamp(i,0,Math.max(0,v.length-1)); const offset = index*(CARD_W+GAP); rail.style.transform=`translateX(${-offset}px)`; updateButtons(); }
    prevBtn.addEventListener('click',()=>snapTo(index-1));
    nextBtn.addEventListener('click',()=>snapTo(index+1));

    /* ===== Filters ===== */
    const filterButtons = document.querySelectorAll('.filter-btn');
    const allCards = Array.from(rail.querySelectorAll('.tcard'));
    function applyFilter(filter){
      allCards.forEach(card => card.classList.toggle('is-hidden', !(filter==='all'||card.dataset.category===filter)));
      snapTo(0);
    }
    filterButtons.forEach(btn=>{
      btn.addEventListener('click',()=>{
        filterButtons.forEach(b=>b.classList.remove('active'));
        btn.classList.add('active');
        applyFilter(btn.getAttribute('data-filter'));
      });
    });
    applyFilter('all');

    /* ===== Secret Burrow: Facebook share with quote ===== */
    const burrowBtn = document.getElementById('burrowShare');
    burrowBtn?.addEventListener('click',(e)=>{
      e.preventDefault();
      const shareUrl = 'https://parkerpixie.netlify.app/parkerpixiepixel/';
      const quote = "I just checked out ParkerPixie's brand new website and I won a prize! Check it out yourself if you are looking for playful, ADHD-friendly planning templates.";
      const fb = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(shareUrl)}&quote=${encodeURIComponent(quote)}`;
      window.open(fb,'_blank','noopener,noreferrer,width=720,height=540');
    });

    /* ===== Copy share link (black button) ===== */
    const copyBtn = document.getElementById('copyShareLink');
    const noteEl  = document.getElementById('copyNote');
    copyBtn?.addEventListener('click', async () => {
      try{
        await navigator.clipboard.writeText('https://parkerpixie.netlify.app/parkerpixiepixel/');
        noteEl.hidden = false; setTimeout(()=>noteEl.hidden=true, 1500);
      }catch(_){
        const ta = document.createElement('textarea');
        ta.value = 'https://parkerpixie.netlify.app/parkerpixiepixel/';
        document.body.appendChild(ta); ta.select(); document.execCommand?.('copy'); document.body.removeChild(ta);
        noteEl.hidden = false; setTimeout(()=>noteEl.hidden=true, 1500);
      }
    });
  </script>
</body>
</html>
