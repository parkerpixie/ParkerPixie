---
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Contact ParkerPixie</title>
    <link rel="icon" href="/CapyGoldIcon.png" />

    <style>
      :root {
        --bg: #000;
        --ink: #fff;
        --muted: #cbbde7;
        --brand: #8a5dff;
      }
      html, body {
        margin: 0;
        padding: 0;
        background: var(--bg);
        color: var(--ink);
        font-family: system-ui, sans-serif;
      }
      .wrap {
        max-width: 900px;
        margin: auto;
        padding: 2rem;
      }
      h1 {
        font-size: 2.2rem;
        margin-bottom: 1rem;
      }
      p {
        font-size: 1.1rem;
        line-height: 1.6;
        color: var(--muted);
      }
      img {
        display: block;
        max-width: 100%;
        height: auto;
        margin: 2rem 0;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.6);
      }
      a {
        color: var(--brand);
        text-decoration: none;
      }
      footer {
        margin-top: 3rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(255, 255, 255, 0.2);
        color: var(--muted);
        text-align: center;
        font-size: 0.95rem;
      }
    </style>
  </head>

  <body>
    <main class="wrap">
      <h1>Contact ParkerPixie</h1>
      <p>
        Thanks for stopping by! 💖 Whether you want to chat about cozy crochet, 
        collab on gaming streams, or just say hi, I’d love to hear from you.
      </p>

      <img src="/ParkerPawsPic" alt="Amigurumi ParkerPixie creation" />

      <p>
        You can reach me by email at 
        <a href="mailto:your@email.com">your@email.com</a>, 
        or connect with me on socials.
      </p>

      <footer>
        © <span id="y"></span> ParkerPixie · Sass · Shimmer · Sparkle
      </footer>
    </main>

    <script>
      document.getElementById("y").textContent = new Date().getFullYear();
    </script>
  </body>
</html>
