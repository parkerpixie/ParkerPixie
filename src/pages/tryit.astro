---
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ParkerPixiePixel • Templates & Designs</title>
  <meta name="description" content="Free ADHD-friendly templates, planners, and graphics designed by ParkerPixie — playful, practical, and easy to use." />
  <link rel="icon" href="/CapyGoldIcon.png" />

  <style>
    :root {
      --brand:#8a5dff;
      --brand-deep:#4b2fb5;
      --gold:#D8AA3B;
      --ink:#f7f3ff;
      --muted:#bdb5d9;
      --bg1:#0f0b14;
      --bg2:#130e1f;
      --bg3:#100b1a;
    }

    body {
      margin:0;
      font-family:'Segoe UI', sans-serif;
      background:var(--bg1);
      color:var(--ink);
    }

    header {
      padding:1rem 2rem;
      background:var(--bg2);
      display:flex;
      justify-content:space-between;
      align-items:center;
    }

    header a {
      color:var(--gold);
      margin-left:1rem;
      font-weight:600;
      text-decoration:none;
    }

    /* HERO */
    .hero {
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      text-align:center;
      padding:5rem 2rem;
      background:var(--bg3);
    }

    .hero h1 {
      font-size:2.8rem;
      font-weight:700;
      color:var(--gold);
      margin-bottom:1rem;
    }

    .hero p {
      max-width:700px;
      font-size:1.2rem;
      color:var(--muted);
      margin-bottom:2rem;
    }

    .btn-primary {
      background:var(--brand);
      color:#fff;
      padding:0.9rem 1.8rem;
      border-radius:9999px;
      font-weight:600;
      text-decoration:none;
    }

    /* GRID */
    .templates {
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(300px,1fr));
      gap:2rem;
      padding:3rem 2rem;
    }

    .card {
      background:var(--bg2);
      border-radius:1rem;
      overflow:hidden;
      box-shadow:0 8px 18px rgba(0,0,0,0.25);
      transition:transform 0.2s;
      position:relative;
    }
    .card:hover { transform:translateY(-5px); }

    .card img {
      width:100%;
      height:200px;
      object-fit:cover;
    }

    .category {
      position:absolute;
      top:15px;
      left:15px;
      background:var(--brand-deep);
      color:white;
      padding:0.4rem 0.8rem;
      font-size:0.75rem;
      border-radius:9999px;
      font-weight:600;
    }

    .card-body {
      padding:1.2rem;
    }
    .card-body h3 {
      margin:0 0 0.5rem;
      font-size:1.3rem;
      color:var(--gold);
    }
    .card-body p {
      font-size:0.95rem;
      color:var(--muted);
    }

    .save-btn {
      display:inline-block;
      margin-top:1rem;
      background:var(--brand);
      color:#fff;
      padding:0.6rem 1.2rem;
      border-radius:9999px;
      font-weight:600;
      text-decoration:none;
    }

    footer {
      text-align:center;
      padding:2rem;
      color:var(--muted);
      font-size:0.85rem;
    }
  </style>
</head>
<body>

  <!-- ✅ Consistent Header -->
  <header>
    <a href="/">ParkerPixie ✨</a>
    <nav>
      <a href="/about">About</a>
      <a href="/blog">Blog</a>
      <a href="/contact">Contact</a>
    </nav>
  </header>

  <!-- 🌟 Hero -->
  <section class="hero">
    <h1>ParkerPixiePixel</h1>
    <p>
      I design playful, ADHD-friendly templates and tools that transform everyday planning 
      into something beautiful and engaging. By blending creativity with organization, 
      I help families (starting with my own!) stay on track without losing the magic.
    </p>
    <a href="https://your-google-form-link.com" class="btn-primary">Request a Graphic</a>
  </section>

  <!-- 🪄 Templates Grid -->
  <section class="templates">
    <div class="card">
      <span class="category">Meal Planning</span>
      <img src="/img/sample1.jpg" alt="Meal Planner Template" />
      <div class="card-body">
        <h3>Weekly Meal Planner</h3>
        <p>A simple ADHD-friendly planner with room for meals, snacks, and grocery notes.</p>
        <a href="#" class="save-btn">Save</a>
      </div>
    </div>

    <div class="card">
      <span class="category">Travel</span>
      <img src="/img/sample2.jpg" alt="Travel Itinerary Template" />
      <div class="card-body">
        <h3>Road Trip Itinerary</h3>
        <p>Plan your stops, sights, and snacks with this whimsical adventure map.</p>
        <a href="#" class="save-btn">Save</a>
      </div>
    </div>

    <div class="card">
      <span class="category">Home Management</span>
      <img src="/img/sample3.jpg" alt="Chore Chart Template" />
      <div class="card-body">
        <h3>Family Chore Chart</h3>
        <p>A colorful, easy-to-read chart that keeps chores on track without nagging.</p>
        <a href="#" class="save-btn">Save</a>
      </div>
    </div>
  </section>

  <footer>
    © {year} ParkerPixie • Sparkles, Pixels & Organization
  </footer>
</body>
</html>
      letter-spacing:.2px;

      /* default fallback (used if images not present) */
      background: radial-gradient(1200px 800px at 10% 0%, rgba(255,255,255,1) 0%, var(--bg) 60%), var(--bg);
      background-attachment: fixed;
    }

    /* class toggles are added by JS below: .bg-loaded keeps the image; .bg-missing uses the gradient above */
    body.bg-loaded { }
    img{max-width:100%; display:block}
    a{color:var(--brand); text-decoration:none}
    a:hover{text-decoration:underline}

    .wrap{max-width:1100px; margin:auto; padding:22px}

    header{ position:relative; border-radius: var(--radius-lg); overflow:hidden; box-shadow: var(--glow); background: rgba(255,255,255,0.9); }
    .hero{display:grid; grid-template-columns: 1.1fr .9fr; gap:20px; align-items:center; padding:32px 28px}
    @media (max-width: 880px){ .hero{grid-template-columns:1fr; padding:24px} }

    .hero-copy h1{margin:0 0 8px; font-size: clamp(28px, 3.6vw, 44px); letter-spacing:.3px}
    .hero-copy p{margin:0 0 16px; color:var(--subtle)}
    .badges{display:flex; gap:10px; flex-wrap:wrap; margin:10px 0 18px}
    .badge{background:var(--panel); border:1px solid #eee; padding:6px 10px; border-radius:999px; font-size:12px; color:var(--subtle)}
    .cta-row{display:flex; gap:12px; flex-wrap:wrap}
    .btn{display:inline-block; padding:12px 16px; border-radius:12px; font-weight:600; border:1px solid transparent; box-shadow: var(--glow)}
    .btn.primary{background: var(--brand); color:#fff}
    .btn.primary:hover{filter:brightness(1.05)}
    .btn.ghost{background:#fff; border-color:#eee; color:var(--ink)}

    .hero-art{position:relative}
    .cloud{ position:absolute; inset:auto auto 10% -10%; width:120%; height:70%; z-index:0; background: radial-gradient(300px 140px at 20% 30%, rgba(124,106,200,.22), transparent 60%), radial-gradient(360px 160px at 80% 60%, rgba(231,179,194,.20), transparent 60%); filter: blur(24px); }
    .hero-img{position:relative; z-index:1; border-radius: 20px; box-shadow: var(--glow); border: 6px solid #fff; width:100%; height:auto; max-width:360px; object-fit:cover}

    section{margin:28px 0}
    .card{background:var(--panel); border:1px solid #eee; border-radius: var(--radius); box-shadow: var(--glow)}
    .section-head{display:flex; align-items:baseline; justify-content:space-between; gap:12px; margin:0 0 14px}
    .section-head h2{margin:0; font-size: clamp(20px, 2.6vw, 28px)}
    .section-head p{margin:4px 0 0; color:var(--subtle)}

    .iotm{display:grid; grid-template-columns: 1fr 1fr; gap:22px; padding:22px}
    @media (max-width: 820px){ .iotm{grid-template-columns:1fr} }
    .price{font-size: clamp(22px, 3vw, 28px); font-weight:800; color:var(--brand)}
    .chips{display:flex; gap:8px; flex-wrap:wrap}
    .chip{padding:6px 10px; border-radius:999px; background:var(--brand-2); color:#4b3f7f; font-size:12px}
    .iotm .btn{margin-top:10px}

    .grid{display:grid; grid-template-columns: repeat(12, 1fr); gap:14px}
    .g-item{grid-column: span 4; border-radius:16px; overflow:hidden; border:1px solid #eee; background:#fff; box-shadow: var(--glow)}
    .g-item img{aspect-ratio:1/1; object-fit:cover}
    .g-item .cap{padding:10px; font-size:14px; color:var(--subtle)}
    @media (max-width: 900px){ .g-item{grid-column: span 6} }
    @media (max-width: 560px){ .g-item{grid-column: span 12} }

    .steps{display:grid; grid-template-columns: repeat(3,1fr); gap:14px; padding:24px}
    @media (max-width: 800px){ .steps{grid-template-columns:1fr} }
    .step{padding:16px; border:1px solid #eee; border-radius:16px; background:#fff}
    .k{display:inline-grid; place-items:center; width:34px; height:34px; border-radius:10px; background:var(--brand-2); color:#4b3f7f; font-weight:700; margin-bottom:6px}

    details{border:1px solid #eee; border-radius:12px; padding:12px 14px; background:#fff}
    details + details{margin-top:10px}
    summary{cursor:pointer; font-weight:600}
    summary::-webkit-details-marker{display:none}

    footer{margin:26px 0 40px; color:var(--subtle); text-align:center}

    /* --- NEW: pill/bubble links for maker resources --- */
    .pillwrap{display:flex; gap:14px; flex-wrap:wrap; margin-top:10px}
    .pilllink{
      display:inline-flex; align-items:center; justify-content:center;
      padding:10px 16px; border-radius:999px; background:var(--brand-2);
      color:#4b3f7f; font-weight:600; white-space:nowrap; border:1px solid #e7defc;
      box-shadow: 0 3px 10px rgba(124,106,200,.18);
      transition: transform .08s ease, box-shadow .12s ease;
    }
    .pilllink:hover{ text-decoration:none; transform: translateY(-1px); box-shadow: 0 6px 16px rgba(124,106,200,.22); }
    .pilllink:focus{ outline:2px solid #c4b5fd; outline-offset:2px }

    /* fallback styles if image not present */
    body.bg-missing header{ background: rgba(255,255,255,0.95); }
  </style>
</head>
<body>
  <div class="wrap">
    <header class="card">
      <div class="hero">
        <div class="hero-copy">
          <h1>ParkerPixiePaws — Cozy Amigurumi</h1>
          <p>I make soft, squishy friends stitched with intention and feel like a hug. Amigurumi is more than yarn and hooks--it is a Japanese art form rooted in care, patience, and the joy of gifting. For me, it has become both therapy and expression: a way to honor the people who inspire me, to stitch metaphors into every piece, and to transform yarn into lasting symbols of love. Each creation carries intention, whether it's given to family, friends, or simply made to celebrate a moment of healing.</p>
          <div class="badges">
            <span class="badge">Handmade in Madison, WI</span>
            <span class="badge">Crafted and perfected with YOU in mind</span>
          </div>
          <div class="cta-row">
            <a class="btn primary" href="#commission">Commission an Amigurumi</a>
            <a class="btn ghost" href="#gallery">Browse Amigurumi</a>
          </div>
        </div>
        <div class="hero-art">
          <div class="cloud" aria-hidden="true"></div>

          <!-- try preferred PNG first; onerror fallback to deployed lowercase JPG -->
          <img
            class="hero-img"
            src="/img/ParkerPixiePawProfile.png"
            onerror="this.onerror=null; this.src='/img/parkerpixiepawprofile.jpg';"
            alt="ParkerPixiePaws profile collage of cozy amigurumi." />
        </div>
      </div>
    </header>

    <section id="iotm" class="card">
      <div class="iotm">
        <div>
          <div class="section-head">
            <h2>✨ Item of the Month</h2>
          </div>
          <p>This month: <strong>Pocket Pumpkins</strong> — plump, palm-sized cuties perfect for fall trays, teacher gifts, or anxiety squishes. Just pick your favorite color!</p>
          <div class="chips">
            <span class="chip">Made to order</span>
            <span class="chip">Ships in 3–5 days</span>
            <span class="chip">Approx. 3.5&quot; wide</span>
          </div>
          <p class="price">$15 • Shipping available at shipping costs</p>
          <a class="btn primary" href="https://forms.gle/YOUR_COMMISSION_FORM" target="_blank" rel="noopener">Order a Pumpkin</a>
          <p style="margin-top:8px; color:var(--subtle); font-size:14px">Limited batch. When sold out, the button will be disabled.</p>
        </div>
        <div>
          <img src="/img/pumpkin.jpg" alt="Hand holding a crocheted pumpkin in front of a pile of amigurumi pumpkins." />
        </div>
      </div>
    </section>

    <section id="gallery" aria-labelledby="gal-h">
      <div class="section-head"><h2 id="gal-h">✨ Handmade Highlights & Pixie Projects</h2><p>Textures, colors, and cozy vibes.</p></div>
      <div class="grid">
        <figure class="g-item"><img src="/img/Bejeweled.jpg" alt="Amigurumi peacock with jeweled tail."><figcaption class="cap">Bejeweled the Peacock</figcaption></figure>
        <figure class="g-item"><img src="/img/bonnie.jpg" alt="Amigurumi chicken in cream and gold."><figcaption class="cap">Bonnie the Chicken</figcaption></figure>
        <figure class="g-item"><img src="/img/gilligan.jpg" alt="Amigurumi character Gilligan."><figcaption class="cap">Gilligan the Pelican</figcaption></figure>
        <figure class="g-item"><img src="/img/ginny.jpg" alt="Amigurumi character Ginny."><figcaption class="cap">Ginny the Giraffe</figcaption></figure>
        <figure class="g-item"><img src="/img/grogu.jpg" alt="Amigurumi Grogu with big ears."><figcaption class="cap">Grogu!</figcaption>
        </figure><figure class="g-item"><img src="/img/Jade.jpg" alt="Amigurumi succulent planter"><figcaption class="cap">Jade the Succulent</figcaption></figure>
        </figure><figure class="g-item"><img src="/img/Aurora.jpg" alt="Amigurumi Puffin in cloud background."><figcaption class="cap">Aurora the Puffin</figcaption></figure>
        </figure><figure class="g-item"><img src="/img/Buster.jpg" alt="Amigurumi dog with big ears."><figcaption class="cap">Buster the Dog</figcaption></figure>
        </figure><figure class="g-item"><img src="/img/Petunia.jpg" alt="Amigurumi Pink Hippo with Ballerina Skirt."><figcaption class="cap">Petunia the Ballerina Hippo</figcaption></figure>
        </figure><figure class="g-item"><img src="/img/Whiskers.jpg" alt="Amigurumi Orange cat with big ears."><figcaption class="cap">Whiskers the Cat</figcaption></figure>
      </div>
    </section>

    <section id="commission" class="card" aria-labelledby="how-h">
      <div class="section-head"><h2 id="how-h">How Commissions Work</h2><p>Simple, cozy steps.</p></div>
      <div class="steps">
        <div class="step"><div class="k">1</div><strong>Tell me your idea.</strong><br/>Animal, vibe, size, colors — or show a reference pic.</div>
        <div class="step"><div class="k">2</div><strong>I confirm details + price.</strong><br/>Most are complete within 2 to 3 weeks! Give me some damn time to stitch, huh???</div>
        <div class="step"><div class="k">3</div><strong>I Stitch, I deliver.</strong><br/>You can pick it up, arrange to meet, or I can ship it to you if you agree to pay the cost.</div>
      </div>
      <div style="padding: 0 14px 18px">
        <a class="btn primary" href="https://forms.gle/YOUR_COMMISSION_FORM" target="_blank" rel="noopener">Start a Commission</a>
        <a class="btn ghost" href="#faq">Read FAQs</a>
      </div>
    </section>

    <!-- UPDATED: Maker resources with lavender pill/bubble links -->
    <section id="makers" class="card" aria-labelledby="makers-h" style="padding:18px">
      <div class="section-head">
        <h2 id="makers-h">Already know how to crochet? Looking for inspiration?</h2>
      </div>
      <p style="margin:0 0 10px; color:var(--subtle)">Here are some of my favorite places for patterns and yarn:</p>
      <div class="pillwrap" role="list">
        <a class="pilllink" role="listitem" href="https://www.ravelry.com/account/login" target="_blank" rel="noopener">Ravelry</a>
        <a class="pilllink" role="listitem" href="https://hobbii.com/" target="_blank" rel="noopener">Hobbii</a>
        <a class="pilllink" role="listitem" href="https://www.knitandsip.com/" target="_blank" rel="noopener">The Sow’s Ear — Verona, WI</a>
        <a class="pilllink" role="listitem" href="https://www.amigurumi.com/search/free/" target="_blank" rel="noopener">Amigurumi.com</a>
        <a class="pilllink" role="listitem" href="https://amenagerieofstitchesblog.com/" target="_blank" rel="noopener">A Menagerie of Stitches</a>
        <a class="pilllink" role="listitem" href="https://www.mariskavos.nl/category/free-amigurumi-patterns/" target="_blank" rel="noopener">DIY Fluffies</a>
        <a class="pilllink" role="listitem" href="https://littlecrochetfarm.com/" target="_blank" rel="noopener">Little Crochet Farm</a>
      </div>
    </section>

    <!-- Materials & Care stays as its own section, below -->
    <section class="card" aria-labelledby="care-h" style="padding:18px">
      <div class="section-head"><h2 id="care-h">Materials & Care</h2><p>Made for snuggles and display.</p></div>
      <ul>
        <li>Premium acrylic/cotton yarns; hypoallergenic, pet-safe stuffing.</li>
        <li>Spot clean with cool water + mild soap; air dry. Avoid machine wash.</li>
        <li>Not intended for children under 3 (small parts possible).</li>
      </ul>
    </section>

    <section id="faq" aria-labelledby="faq-h">
      <div class="section-head"><h2 id="faq-h">FAQ</h2><p>Your top questions, answered.</p></div>
      <details>
        <summary>How big are your pieces?</summary>
        <p>As big or as small as you want them to be. You are paying for my time and mostly yarn — the only limit is likely your pocketbook.</p>
      </details>
      <details>
        <summary>Do you ship outside the US?</summary>
        <p>Shipping is available at shipping costs. If you’re international, note it on the form and I’ll see what’s possible.</p>
      </details>
      <details>
        <summary>Can I rush an order?</summary>
        <p>Sometimes! Add your date in the form and I’ll confirm what’s realistic before you pay.</p>
      </details>
    </section>

    <footer>
      <small>© <span id="yr"></span> ParkerPixiePaws • Made with coziness in Madison, WI</small>
    </footer>
  </div>

  <script>
    // Year
    document.getElementById('yr').textContent = new Date().getFullYear();

    // Background loader: attempt to load the site-wide background image(s).
    (function(){
      var desktop = '/img/pawback.png';
      var mobile = '/img/pawbackmobile.png';
      var chosen = (window.innerWidth <= 768) ? mobile : desktop;
      var img = new Image();
      img.onload = function(){
        // we have the image, apply as body background
        document.body.classList.add('bg-loaded');
        document.body.style.backgroundImage = 'url(\"' + chosen + '\")';
        document.body.style.backgroundSize = 'cover';
        document.body.style.backgroundPosition = 'center';
        document.body.style.backgroundRepeat = 'no-repeat';
      };
      img.onerror = function(){
        // image missing — keep the gradient fallback; add a class for any minor adjustments
        document.body.classList.add('bg-missing');
      };
      img.src = chosen;
    })();
  </script>
</body>
</html>
