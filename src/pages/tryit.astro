---
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Parker Pins & Glitter Wall • ParkerPixiePixel</title>
  <meta name="description" content="Parker Pins & Glitter Wall — a filterable, scrollable gallery of sparkly Parker things: pictures, quotes, videos, memes, and templates." />
  <link rel="icon" href="/CapyGoldIcon.png" />

  <style>
    :root{
      --brand:#8a5dff;
      --brand-deep:#4b2fb5;
      --gold:#D8AA3B;
      --ink:#2c2c2c;
      --muted:#5a5a5a;
      --bg-card:#f5f1eb;
      --bg-bubble:#e8dccd;
      --glass:rgba(255,255,255,0.7);
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:"Segoe UI", system-ui, Arial, sans-serif;
      color:var(--ink);
      min-height:100vh;
    }

    /* Background video behind everything */
    .bg-video{
      position:fixed; inset:0;
      width:100%; height:100%;
      object-fit:cover; z-index:-1;
    }

    header{
      display:flex; align-items:center; justify-content:space-between;
      padding:1rem 2rem; background:var(--glass); backdrop-filter:blur(8px);
      position:sticky; top:0; z-index:5;
    }
    header a{ color:var(--brand-deep); text-decoration:none; font-weight:700 }
    header nav a{ margin-left:1rem; color:var(--brand-deep); font-weight:600; text-decoration:none }

    /* HERO */
    .hero{
      padding:4rem 1.5rem 2rem;
      position:relative;
      z-index:2;
    }
    .hero-bubble{
      max-width:1100px;
      margin:0 auto;
      background:rgba(232,220,205,0.92); /* beige bubble w/ alpha */
      color:var(--ink);
      border:1px solid rgba(0,0,0,0.06);
      border-radius:24px;
      padding:2rem 1.75rem;
      box-shadow:0 18px 40px rgba(0,0,0,0.25);
      backdrop-filter:blur(6px);
    }
    .hero-bubble h1{
      margin:0 0 1rem;
      font-size:clamp(2.2rem, 6vw, 4rem);
      line-height:1.15;
      color:var(--brand-deep);
      letter-spacing:.3px;
    }
    .hero-bubble p{
      margin:0 0 1rem;
      font-size:clamp(1.05rem, 2.2vw, 1.2rem);
      color:#2b2b2b;
    }
    .cta{
      display:inline-block; margin-top:.25rem;
      background:var(--brand);
      color:#fff; text-decoration:none; font-weight:800;
      padding:0.85rem 1.25rem; border-radius:9999px;
      box-shadow:0 6px 18px rgba(0,0,0,0.25);
      transition:.15s ease-in-out;
    }
    .cta:hover{ opacity:.95; transform:translateY(-1px) }

    /* Filter bubbles (always visible, toggle active) */
    .filters{
      max-width:1100px; margin:0 auto; padding:0 1.5rem 0.5rem;
      display:flex; flex-wrap:wrap; gap:.5rem;
    }
    .filter-btn{
      border:0; cursor:pointer; font-weight:700;
      background:var(--bg-bubble); color:var(--brand-deep);
      padding:.55rem 1rem; border-radius:9999px; transition:.2s;
    }
    .filter-btn:hover{ background:var(--brand); color:#fff }
    .filter-btn.active{ background:var(--brand); color:#fff; transform:scale(1.05) }

    /* Scroller shell */
    .scroller-wrap{
      position:relative; max-width:1100px; margin:1rem auto 2.25rem; padding:0 2.75rem; /* room for arrows */
    }
    .scroller{
      display:grid;
      grid-auto-flow:column;
      grid-auto-columns:minmax(260px,320px); /* card width range */
      gap:1.25rem;
      overflow-x:auto;
      scroll-snap-type:x mandatory;
      padding:1rem .5rem 1.25rem;
      scrollbar-width:thin;
    }
    .scroller::-webkit-scrollbar{ height:10px }
    .scroller::-webkit-scrollbar-thumb{ background:rgba(0,0,0,.25); border-radius:999px }

    /* Card */
    .card{
      background:var(--bg-card); border-radius:1rem;
      box-shadow:0 10px 24px rgba(0,0,0,.25);
      display:flex; flex-direction:column; scroll-snap-align:start;
      transition:opacity .35s ease, transform .35s ease;
      opacity:1; transform:scale(1);
      overflow:hidden;
    }
    .card.hide{ opacity:0; transform:scale(.95); pointer-events:none }
    .card img{
      width:100%; aspect-ratio:1/1; object-fit:contain; background:#fff; display:block;
    }
    .card-body{ padding:1rem 1rem 1.1rem; display:flex; flex-direction:column; gap:.4rem; min-height:160px }
    .card-body h3{ margin:0; font-size:1.1rem; color:var(--brand-deep) }
    .card-body p{ margin:0; color:var(--muted); font-size:.95rem; flex:1 }
    .save-btn{
      align-self:flex-start; text-decoration:none; font-weight:700;
      background:var(--brand); color:#fff; padding:.55rem 1rem; border-radius:9999px;
    }

    /* Arrows */
    .arrow{
      position:absolute; top:50%; translate:0 -50%;
      width:42px; height:42px; border-radius:50%;
      display:grid; place-items:center; border:0; cursor:pointer;
      background:var(--glass); backdrop-filter:blur(8px);
      box-shadow:0 6px 16px rgba(0,0,0,.25);
      color:#222;
    }
    .arrow:hover{ background:#fff }
    .prev{ left:.75rem } .next{ right:.75rem }

    footer{ text-align:center; color:#fff; text-shadow:0 2px 4px rgba(0,0,0,.6); padding:2rem 1rem }
  </style>
</head>
<body>

  <!-- Background video (DON'T TOUCH 😉) -->
  <video class="bg-video" autoplay muted loop playsinline poster="/img/hero-fallback.jpg">
    <source src="/videos/pixelbackground.webM" type="video/webm" />
    <source src="/videos/Pixelbackground.mp4" type="video/mp4" />
  </video>

  <header>
    <a href="/">ParkerPixie ✨</a>
    <nav>
      <a href="/about">About</a>
      <a href="/blog">Blog</a>
      <a href="/contact">Contact</a>
    </nav>
  </header>

  <!-- Parker Pins & Glitter Wall — now inside a beige bubble -->
  <section class="hero">
    <div class="hero-bubble">
      <h1>Parker Pins & Glitter Wall</h1>
      <p>
        Welcome to the chaos. This is my little digital corkboard of random everything — pictures, quotes, videos, memes, and whatever shiny thing caught my attention before I got distracted. Think of it as part Pinterest, part "oh wow this photo is amazing," part Facebook throwback, and part "if I don't put it here, it's gone forever."
      </p>
      <p>
        Basically, it’s a peek into the glittery, squirrel-chasing, capybara-calm-seeking, peacock-sparkling swirl that is my brain. Sometimes cozy, sometimes chaotic, always a little extra.
      </p>
      <p>
        So check back whenever — you never know what I’ll decide deserves a spot on the wall. 💫
      </p>
      <a class="cta" href="mailto:ParkerPixieDust@gmail.com?subject=I%20have%20something%20you%27ll%20love!&body=Hi%20Parker%2C%20I%20found%20something%20for%20your%20Pins%20%26%20Glitter%20Wall%3A%20">
        HAVE SOMETHING YOU KNOW I WOULD LOVE? SHARE IT HERE!
      </a>
    </div>
  </section>

  <!-- Filter bubbles -->
  <div class="filters" aria-label="Filter categories">
    <button class="filter-btn active" data-filter="all">All</button>
    <button class="filter-btn" data-filter="sparkle">Sparkle/Sass/Fun</button>
    <button class="filter-btn" data-filter="recovery">Recovery/Growth</button>
    <button class="filter-btn" data-filter="brands">PixiePaws/Pop/Pixel</button>
    <button class="filter-btn" data-filter="adhd">ADHD-Friendly</button>
    <button class="filter-btn" data-filter="home">Home</button>
    <button class="filter-btn" data-filter="travel">Travel</button>
    <button class="filter-btn" data-filter="banner">Banners</button>
    <button class="filter-btn" data-filter="art">Artwork</button>
    <button class="filter-btn" data-filter="meal">Meal Planning</button>
  </div>

  <!-- Horizontal scroller with arrows -->
  <section class="scroller-wrap">
    <button class="arrow prev" aria-label="Scroll left" type="button">‹</button>
    <div class="scroller" id="pins-scroller" aria-live="polite">
      <!-- Each card: preview is 512x512 square; Save -> full image -->
      <article class="card" data-category="sparkle">
        <img src="/img/dummy-512x512.jpg" alt="Sparkle Sass Glitter Chaos preview" loading="lazy" />
        <div class="card-body">
          <h3>Sparkle • Sass • Glitter</h3>
          <p>Whimsical words wall art for instant dopamine.</p>
          <a class="save-btn" href="/img/dummy-1200x800.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="recovery">
        <img src="/img/dummy-512x512.jpg" alt="Recovery Growth Healing preview" loading="lazy" />
        <div class="card-body">
          <h3>Recovery • Growth</h3>
          <p>Gentle reminders for self-compassion and restart energy.</p>
          <a class="save-btn" href="/img/dummy-1560x1440.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="brands">
        <img src="/img/dummy-512x512.jpg" alt="PixiePaws PixiePop PixiePixel preview" loading="lazy" />
        <div class="card-body">
          <h3>PixiePaws • PixiePop • PixiePixel</h3>
          <p>Brand badges and playful identity bits.</p>
          <a class="save-btn" href="/img/dummy-1920x1080.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="adhd">
        <img src="/img/dummy-512x512.jpg" alt="ADHD Focus Dashboard preview" loading="lazy" />
        <div class="card-body">
          <h3>Focus Dashboard</h3>
          <p>Color-coded blocks that calm the chaos.</p>
          <a class="save-btn" href="/img/dummy-300x300.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="home">
        <img src="/img/dummy-512x512.jpg" alt="Family Chore Chart preview" loading="lazy" />
        <div class="card-body">
          <h3>Family Chore Chart</h3>
          <p>Easy to read, nag-free home helper.</p>
          <a class="save-btn" href="/img/dummy-600x400.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="banner">
        <img src="/img/dummy-512x512.jpg" alt="Social Share Banner preview" loading="lazy" />
        <div class="card-body">
          <h3>Social Share Banner</h3>
          <p>OG-perfect 1200×628 for crisp sharing.</p>
          <a class="save-btn" href="/img/dummy-1200x628.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="art">
        <img src="/img/dummy-512x512.jpg" alt="Hero Canvas preview" loading="lazy" />
        <div class="card-body">
          <h3>Hero Canvas</h3>
          <p>HD hero backdrop without blur.</p>
          <a class="save-btn" href="/img/dummy-1920x1080.jpg" download>Save</a>
        </div>
      </article>

      <article class="card" data-category="travel">
        <img src="/img/dummy-512x512.jpg" alt="Road Trip Itinerary preview" loading="lazy" />
        <div class="card-body">
          <h3>Road Trip Itinerary</h3>
          <p>Stops, sights, and snacks — adventure map style.</p>
          <a class="save-btn" href="/img/dummy-3840x2160.jpg" download>Save</a>
        </div>
      </article>
    </div>
    <button class="arrow next" aria-label="Scroll right" type="button">›</button>
  </section>

  <footer>&copy; {year} ParkerPixie • Sparkles, Pixels &amp; Organization</footer>

  <script>
    // Filter logic (keeps bubbles visible, animates cards)
    const filterButtons = document.querySelectorAll(".filter-btn");
    const cards = Array.from(document.querySelectorAll(".card"));
    const scroller = document.getElementById("pins-scroller");

    function applyFilter(filter){
      cards.forEach(card=>{
        const match = filter === "all" || card.dataset.category === filter;
        card.classList.toggle("hide", !match);
      });
      // snap to first visible card
      const firstVisible = cards.find(c => !c.classList.contains("hide"));
      if(firstVisible){ firstVisible.scrollIntoView({behavior:"smooth", inline:"start", block:"nearest"}); }
    }

    filterButtons.forEach(btn=>{
      btn.addEventListener("click", ()=>{
        filterButtons.forEach(b=>b.classList.remove("active"));
        btn.classList.add("active");
        applyFilter(btn.getAttribute("data-filter"));
      });
    });

    // Arrow scrolling
    const prev = document.querySelector(".prev");
    const next = document.querySelector(".next");
    function scrollByOne(){
      const card = cards.find(c=>!c.classList.contains("hide"));
      const width = card ? card.getBoundingClientRect().width + 20 : 320;
      return width;
    }
    prev.addEventListener("click", ()=> scroller.scrollBy({left: -scrollByOne(), behavior:"smooth"}));
    next.addEventListener("click", ()=> scroller.scrollBy({left:  scrollByOne(), behavior:"smooth"}));

    // Keyboard support for scroller
    scroller.addEventListener("keydown", (e)=>{
      if(e.key === "ArrowRight") { scroller.scrollBy({left:  scrollByOne(), behavior:"smooth"}); }
      if(e.key === "ArrowLeft")  { scroller.scrollBy({left: -scrollByOne(), behavior:"smooth"}); }
    });
  </script>
</body>
</html>
