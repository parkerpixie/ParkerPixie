<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ParkerPixiePaws • Cozy Amigurumi, Handmade with Heart</title>
  <meta name="description" content="Cozy amigurumi from ParkerPixiePaws — soft, squishy, and stitched with love. Commission a custom friend or grab the Item of the Month for $15!" />

  <!-- Fav / Share -->
  <link rel="icon" href="/CapyGoldIcon.png" />
  <meta property="og:title" content="ParkerPixiePaws — Cozy Amigurumi" />
  <meta property="og:description" content="Soft, squishy friends — stitched with love. Item of the Month just $15." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://parkerpixie.netlify.app/amigurumi/" />
  <!-- If your hero lives at /public/img/hero.png use that absolute URL below -->
  <meta property="og:image" content="https://parkerpixie.netlify.app/img/hero.png" />
  <meta name="twitter:card" content="summary_large_image" />

  <style>
    /* ——— ParkerPixiePaws: cozy, calm, celestial ——— */
    :root{
      --bg: #faf7fb;          /* soft lilac milk */
      --panel: #ffffff;       /* card surface */
      --ink: #3a3042;         /* deep violet-brown */
      --subtle: #6e6274;      /* muted caption */
      --brand: #7c6ac8;       /* lavender-indigo */
      --brand-2: #e9d8fd;     /* pale lavender */
      --accent: #e7b3c2;      /* rosy pink */
      --glow: 0 10px 24px rgba(60, 40, 120, .12);
      --radius: 18px;
      --radius-lg: 26px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; background: radial-gradient(1200px 800px at 10% 0%, #fff 0%, var(--bg) 60%), var(--bg);
      color:var(--ink); font: 16px/1.55 system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
      letter-spacing:.2px;
    }
    img{max-width:100%; display:block}
    a{color:var(--brand); text-decoration:none}
    a:hover{text-decoration:underline}

    .wrap{max-width:1100px; margin:auto; padding:22px}

    /* Header */
    header{
      position:relative; border-radius: var(--radius-lg);
      overflow:hidden; box-shadow: var(--glow);
      background: linear-gradient(135deg, #fefcff 0%, #f7f3ff 100%);
    }
    .hero{display:grid; grid-template-columns: 1.1fr .9fr; gap:20px; align-items:center; padding:32px 28px}
    @media (max-width: 880px){ .hero{grid-template-columns:1fr; padding:24px} }

    .hero-copy h1{margin:0 0 8px; font-size: clamp(28px, 3.6vw, 44px); letter-spacing:.3px}
    .hero-copy p{margin:0 0 16px; color:var(--subtle)}
    .badges{display:flex; gap:10px; flex-wrap:wrap; margin:10px 0 18px}
    .badge{background:var(--panel); border:1px solid #eee; padding:6px 10px; border-radius:999px; font-size:12px; color:var(--subtle)}
    .cta-row{display:flex; gap:12px; flex-wrap:wrap}
    .btn{display:inline-block; padding:12px 16px; border-radius:12px; font-weight:600; border:1px solid transparent; box-shadow: var(--glow)}
    .btn.primary{background: var(--brand); color:#fff}
    .btn.primary:hover{filter:brightness(1.05)}
    .btn.ghost{background:#fff; border-color:#eee; color:var(--ink)}

    .hero-art{position:relative}
    .cloud{
      position:absolute; inset:auto auto 10% -10%; width:120%; height:70%; z-index:0;
      background: radial-gradient(300px 140px at 20% 30%, rgba(124,106,200,.22), transparent 60%),
                  radial-gradient(360px 160px at 80% 60%, rgba(231,179,194,.20), transparent 60%);
      filter: blur(24px);
    }
    .hero-img{position:relative; z-index:1; border-radius: 20px; box-shadow: var(--glow); border: 6px solid #fff}

    /* Section shells */
    section{margin:28px 0}
    .card{background:var(--panel); border:1px solid #eee; border-radius: var(--radius); box-shadow: var(--glow)}
    .section-head{display:flex; align-items:baseline; justify-content:space-between; gap:12px; margin:0 0 14px}
    .section-head h2{margin:0; font-size: clamp(20px, 2.6vw, 28px)}
    .section-head p{margin:4px 0 0; color:var(--subtle)}

    /* IOTM */
    .iotm{display:grid; grid-template-columns: 1fr 1fr; gap:22px; padding:22px}
    @media (max-width: 820px){ .iotm{grid-template-columns:1fr} }
    .price{font-size: clamp(22px, 3vw, 28px); font-weight:800; color:var(--brand)}
    .chips{display:flex; gap:8px; flex-wrap:wrap}
    .chip{padding:6px 10px; border-radius:999px; background:var(--brand-2); color:#4b3f7f; font-size:12px}
    .iotm .btn{margin-top:10px}

    /* Gallery */
    .grid{display:grid; grid-template-columns: repeat(12, 1fr); gap:14px}
    .g-item{grid-column: span 4; border-radius:16px; overflow:hidden; border:1px solid #eee; background:#fff; box-shadow: var(--glow)}
    .g-item img{aspect-ratio:1/1; object-fit:cover}
    .g-item .cap{padding:10px; font-size:14px; color:var(--subtle)}
    @media (max-width: 900px){ .g-item{grid-column: span 6} }
    @media (max-width: 560px){ .g-item{grid-column: span 12} }

    /* Steps */
    .steps{display:grid; grid-template-columns: repeat(3,1fr); gap:14px; padding:14px}
    @media (max-width: 800px){ .steps{grid-template-columns:1fr} }
    .step{padding:16px; border:1px solid #eee; border-radius:16px; background:#fff}
    .k{display:inline-grid; place-items:center; width:34px; height:34px; border-radius:10px; background:var(--brand-2); color:#4b3f7f; font-weight:700; margin-bottom:6px}

    /* FAQ */
    details{border:1px solid #eee; border-radius:12px; padding:12px 14px; background:#fff}
    details + details{margin-top:10px}
    summary{cursor:pointer; font-weight:600}
    summary::-webkit-details-marker{display:none}

    footer{margin:26px 0 40px; color:var(--subtle); text-align:center}
  </style>
</head>
<body>
  <div class="wrap">

    <!-- Header / Hero -->
    <header class="card">
      <div class="hero">
        <div class="hero-copy">
          <h1>ParkerPixiePaws — Cozy Amigurumi</h1>
          <p>Soft, squishy friends stitched with intention. I make capybaras, chickens, puffins, pelicans, and custom creatures that feel like a hug.</p>
          <div class="badges">
            <span class="badge">Handmade in Madison, WI</span>
            <span class="badge">ADHD‑friendly ordering</span>
            <span class="badge">Gentle yarn • Pet‑safe stuffing</span>
          </div>
          <div class="cta-row">
            <a class="btn primary" href="#iotm">Item of the Month — $15</a>
            <a class="btn ghost" href="#commission">Commission a Custom</a>
          </div>
        </div>
        <div class="hero-art">
          <div class="cloud" aria-hidden="true"></div>
          <!-- Replace src with your cozy hero photo (lifestyle shot works best) -->
          <img class="hero-img" src="/img/hero.png" alt="A cozy arrangement of handmade amigurumi friends: a capybara with a tiny flower crown, a puffin, and a chicken, all in soft lavender and cream tones."/>
        </div>
      </div>
    </header>

    <!-- Item of the Month -->
    <section id="iotm" class="card">
      <div class="iotm">
        <div>
          <div class="section-head">
            <h2>✨ Item of the Month</h2>
          </div>
          <p>Each month I batch a single design so I can offer it at a sweet price. For September, meet <strong>"Clementine Jr."</strong> — a pocket capybara with a tiny flower crown. Perfect desk buddy, anxiety squish, or gift.</p>
          <div class="chips">
            <span class="chip">Made to order</span>
            <span class="chip">Ships in 3–5 days</span>
            <span class="chip">Approx. 4" tall</span>
          </div>
          <p class="price">$15 • US only</p>
          <!-- Replace href with your Google Form link for Item of the Month orders -->
          <a class="btn primary" href="https://forms.gle/YOUR_IOTM_FORM" target="_blank" rel="noopener">Order September’s IOTM</a>
          <p style="margin-top:8px; color:var(--subtle); font-size:14px">Limited batch. If this month sells out, the form will close automatically.</p>
        </div>
        <div>
          <!-- Swap with IOTM photo -->
          <img src="/img/iotm-clementinejr.png" alt="September Item of the Month: a pocket-size capybara amigurumi wearing a tiny flower crown." />
        </div>
      </div>
    </section>

    <!-- Gallery (swap images as needed) -->
    <section aria-labelledby="gal-h">
      <div class="section-head"><h2 id="gal-h">Recent Makes</h2><p>Textures, colors, and cozy vibes.</p></div>
      <div class="grid">
        <figure class="g-item"><img src="/img/gallery/capy-flower.png" alt="Capybara with flower crown."><figcaption class="cap">Clementine, capybara goddess</figcaption></figure>
        <figure class="g-item"><img src="/img/gallery/chicken.png" alt="Amigurumi chicken in cream and gold."><figcaption class="cap">Bonnie the chicken</figcaption></figure>
        <figure class="g-item"><img src="/img/gallery/puffin.png" alt="Amigurumi puffin with orange beak."><figcaption class="cap">Percy the puffin</figcaption></figure>
        <figure class="g-item"><img src="/img/gallery/pelican.png" alt="Amigurumi pelican with fish detail."><figcaption class="cap">Percival the pelican</figcaption></figure>
        <figure class="g-item"><img src="/img/gallery/peacock.png" alt="Amigurumi peacock with jeweled tail."><figcaption class="cap">Bejeweled the peacock</figcaption></figure>
        <figure class="g-item"><img src="/img/gallery/custom.png" alt="Custom commission trio."><figcaption class="cap">Custom trio (gift set)</figcaption></figure>
      </div>
    </section>

    <!-- How commissions work -->
    <section id="commission" class="card" aria-labelledby="how-h">
      <div class="section-head"><h2 id="how-h">How Commissions Work</h2><p>Simple, ADHD‑friendly steps.</p></div>
      <div class="steps">
        <div class="step"><div class="k">1</div><strong>Tell me your idea.</strong><br/>Animal, vibe, size, colors — or show a reference pic.</div>
        <div class="step"><div class="k">2</div><strong>I confirm details + price.</strong><br/>You’ll get a friendly quote and timeline (most smalls in 1–2 weeks).</div>
        <div class="step"><div class="k">3</div><strong>I stitch & ship.</strong><br/>I’ll share a progress snap, then send tracking when it’s on the way.</div>
      </div>
      <div style="padding: 0 14px 18px">
        <!-- Replace with your separate Google Form for commissions -->
        <a class="btn primary" href="https://forms.gle/YOUR_COMMISSION_FORM" target="_blank" rel="noopener">Start a Commission</a>
        <a class="btn ghost" href="#faq">Read FAQs</a>
      </div>
    </section>

    <!-- Care + Materials -->
    <section class="card" aria-labelledby="care-h" style="padding:18px">
      <div class="section-head"><h2 id="care-h">Materials & Care</h2><p>Made for snuggles and display.</p></div>
      <ul>
        <li>Premium acrylic/cotton yarns; hypoallergenic, pet‑safe stuffing.</li>
        <li>Spot clean with cool water + mild soap; air dry. Avoid machine wash.</li>
        <li>Not intended for children under 3 (small parts possible).</li>
      </ul>
    </section>

    <!-- FAQ -->
    <section id="faq" aria-labelledby="faq-h">
      <div class="section-head"><h2 id="faq-h">FAQ</h2><p>Your top questions, answered.</p></div>
      <details>
        <summary>How big are your pieces?</summary>
        <p>Smalls are ~3–5 inches. Customs can be sized up; mention your preference on the form.</p>
      </details>
      <details>
        <summary>Do you ship outside the US?</summary>
        <p>Right now I ship within the US. If you’re international, note it on the form and I’ll see what’s possible.</p>
      </details>
      <details>
        <summary>Can I rush an order?</summary>
        <p>Sometimes! Add your date in the form and I’ll confirm what’s realistic before you pay.</p>
      </details>
    </section>

    <footer>
      <small>© <span id="yr"></span> ParkerPixiePaws • Made with coziness in Madison, WI</small>
    </footer>
  </div>

  <script>
    document.getElementById('yr').textContent = new Date().getFullYear();
  </script>
</body>
</html>
